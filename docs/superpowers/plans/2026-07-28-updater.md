# Updater do Knobler — plano de implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** O Knobler avisa sozinho quando existe versão nova no GitHub Releases e instala com um clique, pelo brew ou baixando o zip.

**Architecture:** Um `Updater` (ObservableObject singleton) checa a API do GitHub em background, publica um `UpdateState`, e é consumido por dois lugares independentes: o card no notch (via `NotchViewModel`, empurrado pelo `AppDelegate` como já acontece com AirPods/Pomodoro) e o painel Ajustes › Geral (que observa o singleton direto). A instalação escolhe entre `brew upgrade` e download direto sondando `brew list --cask knobler`.

**Tech Stack:** Swift 5, AppKit + SwiftUI, `URLSession`, `Foundation.Process`. Nenhuma dependência nova.

**Spec:** `docs/superpowers/specs/2026-07-28-auto-update-design.md`

## Global Constraints

- Comentários e strings de UI em **pt-BR**.
- Simplificações deliberadas marcadas com `// ponytail:`.
- Nenhuma dependência nova (sem Sparkle, sem SwiftPM novo).
- `Knobler/Updater.swift` **não** pode referenciar `AppSettings`, `NotchViewModel` nem nenhum outro tipo do app — o self-check em `tools/updatercheck.swift` compila esse arquivo isolado. A ligação com preferências e UI acontece no `AppDelegate`.
- Repositório fixo: `luccas-silveira/knobler`. Tap: `luccas-silveira/knobler`.
- Arquivo novo em `Knobler/` exige `xcodegen generate` antes de compilar.
- **Nunca** editar `Knobler.xcodeproj` à mão, nem `MARKETING_VERSION`.
- Build de verificação: `xcodebuild -project Knobler.xcodeproj -scheme Knobler -configuration Debug build`.

---

## Estrutura de arquivos

| Arquivo | Responsabilidade |
|---|---|
| `Knobler/Updater.swift` (novo) | Tudo do domínio de update: modelos, comparação de versão, checagem HTTP, agendamento, instalação, relaunch. Sem UI, sem dependência do resto do app. |
| `tools/updatercheck.swift` (novo) | Self-check das funções puras. Fora do alvo do app. |
| `Knobler/AppSettings.swift` | Ganha a flag `checkForUpdates`. |
| `Knobler/SettingsView.swift` | `GeneralSettingsPane` ganha a seção de versão/atualização. |
| `Knobler/NotchViewModel.swift` | Ganha `update`, `updateCard`, `case update` no `Mode`, e as closures de ação. |
| `Knobler/NotchView.swift` | Ganha `updateCard` (a view) e o tamanho do modo. |
| `Knobler/KnoblerApp.swift` | Fia o `Updater` nos view models e na flag de preferências. |
| `tools/snapshot.sh`, `tools/main.swift` | Cenário visual do card. |

---

### Task 1: Núcleo puro do Updater

Modelos e as três funções puras (comparar versão, limpar markdown, parsear a resposta da API), com self-check rodável. Nada de rede ainda.

**Files:**
- Create: `Knobler/Updater.swift`
- Create: `tools/updatercheck.swift`

**Interfaces:**
- Consumes: nada.
- Produces:
  - `struct Release: Equatable { let version: String; let notes: String; let url: URL; let asset: URL? }`
  - `enum UpdateState: Equatable { case available(Release), installing, failed(String) }`
  - `func isNewer(_ a: String, than b: String) -> Bool`
  - `func stripMarkdown(_ s: String) -> String`
  - `func parseRelease(_ data: Data) throws -> Release?`

- [ ] **Step 1: Escreva o self-check (que ainda não compila)**

Crie `tools/updatercheck.swift`:

```swift
//
//  tools/updatercheck.swift — self-check das funções puras do Updater.
//  NÃO faz parte do alvo do app.
//
//  Rodar:
//  xcrun swiftc -parse-as-library -swift-version 5 \
//    Knobler/Updater.swift tools/updatercheck.swift -o /tmp/updatercheck && /tmp/updatercheck
//

import Foundation

@main
struct UpdaterCheck {
    static func main() {
        testVersionOrdering()
        testVersionEdgeCases()
        testStripMarkdown()
        testParseRelease()
        testParseSkipsDraftAndPrerelease()
        print("✅ updatercheck ok")
    }

    static func testVersionOrdering() {
        assert(isNewer("0.9.0", than: "0.8.4"), "minor maior")
        assert(isNewer("0.10.0", than: "0.9.0"), "10 > 9 numericamente, não alfabeticamente")
        assert(isNewer("1.0.0", than: "0.99.99"), "major domina")
        assert(isNewer("0.8.5", than: "0.8.4"), "patch maior")
        assert(!isNewer("0.8.4", than: "0.9.0"), "menor não é maior")
        assert(!isNewer("0.8.4", than: "0.8.4"), "igual não é maior")
    }

    static func testVersionEdgeCases() {
        assert(isNewer("v0.9.0", than: "0.8.4"), "prefixo v tolerado à esquerda")
        assert(isNewer("0.9.0", than: "v0.8.4"), "prefixo v tolerado à direita")
        assert(!isNewer("abacaxi", than: "0.8.4"), "lixo não é maior")
        assert(!isNewer("0.9", than: "0.8.4"), "sem 3 componentes não é maior")
        assert(!isNewer("0.9.0", than: ""), "vazio não crasha")
    }

    static func testStripMarkdown() {
        let raw = "### Fixed\n- **Ditado** parava de `funcionar`\n\n### Added\n- Card novo\n"
        let clean = stripMarkdown(raw)
        assert(!clean.contains("#"), "cabeçalho sem #")
        assert(!clean.contains("**"), "sem negrito")
        assert(!clean.contains("`"), "sem crase")
        assert(clean.contains("Fixed"), "texto do cabeçalho sobrevive")
        assert(clean.contains("Ditado"), "texto do item sobrevive")
        assert(!clean.hasSuffix("\n"), "sem newline sobrando no fim")
    }

    static func testParseRelease() {
        let json = """
        {
          "tag_name": "v0.9.0",
          "html_url": "https://github.com/luccas-silveira/knobler/releases/tag/v0.9.0",
          "body": "### Fixed\\n- **algo**",
          "draft": false,
          "prerelease": false,
          "assets": [
            {"name": "Knobler-0.9.0.zip",
             "browser_download_url": "https://github.com/luccas-silveira/knobler/releases/download/v0.9.0/Knobler-0.9.0.zip"}
          ]
        }
        """.data(using: .utf8)!
        guard let r = try? parseRelease(json), let release = r else {
            assertionFailure("parse devia ter devolvido um release"); return
        }
        assert(release.version == "0.9.0", "versão sem o v: \(release.version)")
        assert(release.asset?.lastPathComponent == "Knobler-0.9.0.zip", "asset .zip encontrado")
        assert(release.notes.contains("algo"), "notas parseadas")
        assert(!release.notes.contains("**"), "notas já vêm limpas")
    }

    static func testParseSkipsDraftAndPrerelease() {
        func payload(draft: Bool, pre: Bool) -> Data {
            """
            {"tag_name":"v0.9.0",
             "html_url":"https://github.com/luccas-silveira/knobler/releases/tag/v0.9.0",
             "body":"x","draft":\(draft),"prerelease":\(pre),"assets":[]}
            """.data(using: .utf8)!
        }
        assert((try? parseRelease(payload(draft: true, pre: false))) ?? nil == nil, "draft ignorado")
        assert((try? parseRelease(payload(draft: false, pre: true))) ?? nil == nil, "prerelease ignorado")
        // release sem .zip ainda é válido — só não terá caminho de download direto
        let noAsset = try? parseRelease(payload(draft: false, pre: false))
        assert(noAsset??.asset == nil, "sem asset .zip → asset nil, release válido")
    }
}
```

- [ ] **Step 2: Rode e confirme que falha**

```bash
xcrun swiftc -parse-as-library -swift-version 5 \
  Knobler/Updater.swift tools/updatercheck.swift -o /tmp/updatercheck
```

Esperado: FALHA com "no such file or directory: 'Knobler/Updater.swift'".

- [ ] **Step 3: Escreva o núcleo**

Crie `Knobler/Updater.swift`:

```swift
//
//  Updater.swift — descobre e instala novas versões do Knobler.
//
//  Fonte da verdade: o release mais recente em github.com/luccas-silveira/knobler.
//  Este arquivo é deliberadamente autocontido (não conhece AppSettings, o notch,
//  nem a tela de Ajustes) para que tools/updatercheck.swift o compile isolado.
//

import Foundation

/// Uma versão publicada no GitHub Releases.
struct Release: Equatable {
    /// "0.9.0" — sem o "v" da tag.
    let version: String
    /// Notas já limpas de markdown, prontas pro card.
    let notes: String
    /// Página do release (fallback quando não dá pra instalar sozinho).
    let url: URL
    /// O .zip anexado. nil = sem caminho de download direto.
    let asset: URL?
}

enum UpdateState: Equatable {
    case available(Release)
    case installing
    case failed(String)
}

// MARK: - Comparação de versão

/// Quebra "0.9.0" (ou "v0.9.0") em [0, 9, 0]. nil se não for X.Y.Z numérico.
private func versionComponents(_ v: String) -> [Int]? {
    let trimmed = v.hasPrefix("v") ? String(v.dropFirst()) : v
    let parts = trimmed.split(separator: ".", omittingEmptySubsequences: false)
    guard parts.count == 3 else { return nil }
    let numbers = parts.compactMap { Int($0) }
    return numbers.count == 3 ? numbers : nil
}

/// true se `a` é uma versão maior que `b`. Comparação por componente numérico —
/// "0.10.0" > "0.9.0", que a comparação de string erraria. Entrada malformada
/// devolve false: na dúvida, não anunciar update.
func isNewer(_ a: String, than b: String) -> Bool {
    guard let x = versionComponents(a), let y = versionComponents(b) else { return false }
    for (l, r) in zip(x, y) where l != r { return l > r }
    return false
}

// MARK: - Notas do release

/// O body do release vem em markdown (o release.sh copia do CHANGELOG). O card
/// mostra texto puro. ponytail: strip ingênuo (#, **, `) — cobre o que o
/// CHANGELOG usa; se um dia entrar link ou tabela, aí sim vale um parser.
func stripMarkdown(_ s: String) -> String {
    s.split(separator: "\n", omittingEmptySubsequences: false)
        .map { line -> String in
            var l = String(line.drop(while: { $0 == "#" }))
            l = l.replacingOccurrences(of: "**", with: "")
            l = l.replacingOccurrences(of: "`", with: "")
            return l.trimmingCharacters(in: .whitespaces)
        }
        .joined(separator: "\n")
        .trimmingCharacters(in: .whitespacesAndNewlines)
}

// MARK: - Parse da API

private struct APIRelease: Decodable {
    struct Asset: Decodable {
        let name: String
        let browserDownloadUrl: URL
    }
    let tagName: String
    let htmlUrl: URL
    let body: String?
    let draft: Bool
    let prerelease: Bool
    let assets: [Asset]
}

/// Converte a resposta de /releases/latest num Release.
/// Devolve nil para draft/prerelease — nenhum dos dois deve virar aviso.
func parseRelease(_ data: Data) throws -> Release? {
    let decoder = JSONDecoder()
    decoder.keyDecodingStrategy = .convertFromSnakeCase
    let api = try decoder.decode(APIRelease.self, from: data)
    guard !api.draft, !api.prerelease else { return nil }
    let version = api.tagName.hasPrefix("v") ? String(api.tagName.dropFirst()) : api.tagName
    return Release(
        version: version,
        notes: stripMarkdown(api.body ?? ""),
        url: api.htmlUrl,
        asset: api.assets.first { $0.name.hasSuffix(".zip") }?.browserDownloadUrl
    )
}
```

- [ ] **Step 4: Rode o self-check e confirme que passa**

```bash
xcrun swiftc -parse-as-library -swift-version 5 \
  Knobler/Updater.swift tools/updatercheck.swift -o /tmp/updatercheck && /tmp/updatercheck
```

Esperado: `✅ updatercheck ok`, exit 0.

> Nota: `swiftc` sem `-O` mantém os `assert` ativos. Não compile com `-O` aqui.

- [ ] **Step 5: Commit**

```bash
git add Knobler/Updater.swift tools/updatercheck.swift
git commit -m "feat(updater): núcleo de versão, notas e parse do release"
```

---

### Task 2: Checagem remota e agendamento

O `Updater` vira um `ObservableObject` que consulta a API e publica o estado. Sem instalação ainda — o botão vem na Task 3.

**Files:**
- Modify: `Knobler/Updater.swift` (acrescenta a classe ao fim)
- Modify: `Knobler/AppSettings.swift` (flag `checkForUpdates`)

**Interfaces:**
- Consumes: `Release`, `UpdateState`, `isNewer(_:than:)`, `parseRelease(_:)` da Task 1.
- Produces:
  - `final class Updater: ObservableObject`
  - `static let shared: Updater`
  - `@Published private(set) var state: UpdateState?`
  - `private(set) var skippedVersion: String?`
  - `var automatic: Bool` (setada de fora; default `true`)
  - `var installedVersion: String`
  - `func start()`, `func check(force: Bool = false)`, `func skipCurrent()`
  - `var isSkipped: Bool` — true quando o release disponível já foi dispensado

- [ ] **Step 1: Acrescente a classe em `Knobler/Updater.swift`**

Primeiro troque o `import Foundation` do topo do arquivo por:

```swift
import AppKit    // NSApp.terminate no relaunch (Task 3)
import Foundation
```

Depois acrescente a classe ao fim:

```swift
// MARK: - Updater

final class Updater: ObservableObject {
    static let shared = Updater()

    /// nil = nenhuma novidade (ou ainda não checou).
    @Published private(set) var state: UpdateState?

    /// Versão que o usuário mandou embora com "Depois". Persistida: o aviso não
    /// volta a incomodar até sair uma versão diferente.
    private(set) var skippedVersion: String? {
        didSet { UserDefaults.standard.set(skippedVersion, forKey: Self.skippedKey) }
    }

    /// Checagem periódica ligada. Quem manda aqui é o AppDelegate, espelhando
    /// AppSettings.checkForUpdates — o Updater não conhece as preferências.
    var automatic = true

    private static let skippedKey = "updateSkippedVersion"
    private static let lastCheckKey = "updateLastCheck"
    private static let feed = URL(
        string: "https://api.github.com/repos/luccas-silveira/knobler/releases/latest")!
    /// ponytail: 24h fixo. Vira preferência se alguém reclamar.
    private static let interval: TimeInterval = 24 * 60 * 60
    /// Folga pro app terminar de subir antes de gastar rede.
    private static let launchDelay: TimeInterval = 30

    private var timer: Timer?

    private init() {
        skippedVersion = UserDefaults.standard.string(forKey: Self.skippedKey)
    }

    /// Versão rodando agora, do Info.plist. "0.0.0" só em build sem MARKETING_VERSION.
    var installedVersion: String {
        Bundle.main.object(forInfoDictionaryKey: "CFBundleShortVersionString")
            as? String ?? "0.0.0"
    }

    /// true quando existe update disponível mas o usuário já disse "Depois".
    var isSkipped: Bool {
        guard case .available(let release) = state else { return false }
        return release.version == skippedVersion
    }

    /// Agenda a primeira checagem e o ciclo de 24h. Idempotente.
    func start() {
        timer?.invalidate()
        DispatchQueue.main.asyncAfter(deadline: .now() + Self.launchDelay) { [weak self] in
            self?.check()
        }
        let timer = Timer(timeInterval: Self.interval, repeats: true) { [weak self] _ in
            self?.check()
        }
        RunLoop.main.add(timer, forMode: .common)
        self.timer = timer
    }

    /// `force` = o usuário clicou "Verificar agora": ignora o toggle e o intervalo.
    func check(force: Bool = false) {
        guard force || automatic else { return }
        if !force, let last = UserDefaults.standard.object(forKey: Self.lastCheckKey) as? Date,
           Date().timeIntervalSince(last) < Self.interval { return }

        var request = URLRequest(url: Self.feed)
        request.timeoutInterval = 15
        // sem esta linha o GitHub responde com um envelope de mídia legado
        request.setValue("application/vnd.github+json", forHTTPHeaderField: "Accept")

        URLSession.shared.dataTask(with: request) { [weak self] data, response, _ in
            guard let self else { return }
            // Falha de rede e rate limit (403) são silenciosos: não existe update
            // bom o bastante pra justificar um alerta de erro de rede.
            guard let data,
                  let http = response as? HTTPURLResponse, http.statusCode == 200,
                  let release = try? parseRelease(data), let release else { return }
            UserDefaults.standard.set(Date(), forKey: Self.lastCheckKey)
            guard isNewer(release.version, than: self.installedVersion) else {
                DispatchQueue.main.async { self.state = nil }
                return
            }
            DispatchQueue.main.async { self.state = .available(release) }
        }.resume()
    }

    /// "Depois": some do notch, continua nos Ajustes.
    func skipCurrent() {
        guard case .available(let release) = state else { return }
        skippedVersion = release.version
        objectWillChange.send()
    }
}
```

- [ ] **Step 2: Acrescente a flag em `Knobler/AppSettings.swift`**

Junto dos outros `@Published` (na altura de `localAPI`):

```swift
    /// Checagem automática de novas versões (a cada 24h).
    @Published var checkForUpdates: Bool {
        didSet { UserDefaults.standard.set(checkForUpdates, forKey: "checkForUpdates") }
    }
```

E dentro de `private init()`, junto dos outros `flag(...)` (default ligado):

```swift
        checkForUpdates = flag("checkForUpdates")
```

- [ ] **Step 3: Regenere o projeto e compile**

```bash
xcodegen generate
xcodebuild -project Knobler.xcodeproj -scheme Knobler -configuration Debug build 2>&1 | tail -5
```

Esperado: `** BUILD SUCCEEDED **`.

- [ ] **Step 4: Confirme que o self-check da Task 1 continua passando**

```bash
xcrun swiftc -parse-as-library -swift-version 5 \
  Knobler/Updater.swift tools/updatercheck.swift -o /tmp/updatercheck && /tmp/updatercheck
```

Esperado: `✅ updatercheck ok`. (Se falhar com erro de link do AppKit, é sinal de que a classe ficou acoplada a algo do app — reveja.)

- [ ] **Step 5: Verifique a chamada real contra o GitHub**

```bash
curl -s -H "Accept: application/vnd.github+json" \
  https://api.github.com/repos/luccas-silveira/knobler/releases/latest \
  | python3 -c "import json,sys; d=json.load(sys.stdin); print(d['tag_name'], [a['name'] for a in d['assets']])"
```

Esperado: a tag mais recente e o `.zip` — os mesmos campos que `parseRelease` consome.

- [ ] **Step 6: Commit**

```bash
git add Knobler/Updater.swift Knobler/AppSettings.swift project.yml
git commit -m "feat(updater): checagem periódica do GitHub Releases"
```

---

### Task 3: Instalação e relaunch

Os dois caminhos (brew e download direto) e o relaunch. É a tarefa com mais superfície de erro; cada passo falho vira `.failed` com mensagem.

**Files:**
- Modify: `Knobler/Updater.swift` (nova extensão ao fim)

**Interfaces:**
- Consumes: `Updater`, `UpdateState`, `Release` das Tasks 1-2.
- Produces:
  - `func install()` — assíncrona; publica `.installing` e termina em relaunch ou `.failed`
  - `var canInstall: Bool` — false quando não há brew nem asset; a UI mostra "Ver release"
  - `var releaseURL: URL?` — pro botão de fallback

- [ ] **Step 1: Acrescente a extensão em `Knobler/Updater.swift`**

```swift
// MARK: - Instalação

extension Updater {
    /// Caminhos do brew: Apple Silicon primeiro, Intel depois.
    private static var brewPaths: [String] {
        ["/opt/homebrew/bin/brew", "/usr/local/bin/brew"]
    }

    private var brewPath: String? {
        Self.brewPaths.first { FileManager.default.isExecutableFile(atPath: $0) }
    }

    /// O app veio do cask? Se sim, atualizar por fora desincronizaria o Caskroom.
    private var installedViaBrew: Bool {
        guard let brew = brewPath else { return false }
        return run(brew, ["list", "--cask", "knobler"]).status == 0
    }

    /// Substituir o bundle só é seguro em /Applications — num build de
    /// desenvolvimento isso apagaria a árvore de build debaixo do Xcode.
    private var isInApplications: Bool {
        Bundle.main.bundleURL.path.hasPrefix("/Applications/")
    }

    var releaseURL: URL? {
        guard case .available(let release) = state else { return nil }
        return release.url
    }

    /// false = não dá pra instalar sozinho; a UI oferece "Ver release".
    var canInstall: Bool {
        guard case .available(let release) = state else { return false }
        if installedViaBrew { return true }
        return release.asset != nil && isInApplications
    }

    /// Roda um binário e devolve status + saída. Lê até EOF antes do wait para
    /// não travar se a saída encher o buffer do pipe.
    private func run(_ path: String, _ args: [String],
                     env: [String: String] = [:]) -> (status: Int32, out: String, err: String) {
        let p = Process()
        p.executableURL = URL(fileURLWithPath: path)
        p.arguments = args
        if !env.isEmpty {
            var merged = ProcessInfo.processInfo.environment
            env.forEach { merged[$0.key] = $0.value }
            p.environment = merged
        }
        let outPipe = Pipe(), errPipe = Pipe()
        p.standardOutput = outPipe
        p.standardError = errPipe
        do { try p.run() } catch { return (-1, "", "não consegui executar \(path)") }
        let outData = outPipe.fileHandleForReading.readDataToEndOfFile()
        let errData = errPipe.fileHandleForReading.readDataToEndOfFile()
        p.waitUntilExit()
        return (p.terminationStatus,
                String(decoding: outData, as: UTF8.self),
                String(decoding: errData, as: UTF8.self))
    }

    /// Última linha não-vazia — o que vale mostrar de um stderr de 40 linhas.
    private func lastLine(_ s: String) -> String {
        s.split(separator: "\n").last.map(String.init)?
            .trimmingCharacters(in: .whitespaces) ?? "erro desconhecido"
    }

    private func fail(_ message: String) {
        DispatchQueue.main.async { self.state = .failed(message) }
    }

    /// Instala a versão disponível e reinicia o app. Não bloqueia a main queue.
    func install() {
        guard case .available(let release) = state else { return }
        DispatchQueue.main.async { self.state = .installing }
        DispatchQueue.global(qos: .userInitiated).async { [weak self] in
            guard let self else { return }
            let ok = self.installedViaBrew
                ? self.installViaBrew()
                : self.installDirect(release)
            if ok { self.relaunch() }
        }
    }

    // MARK: caminho brew

    private func installViaBrew() -> Bool {
        guard let brew = brewPath else {
            fail("Homebrew não encontrado."); return false
        }
        // ponytail: `git pull` só no tap. `brew update` completo busca todos os
        // taps da máquina e leva ~30s pra descobrir uma linha de versão.
        let repo = run(brew, ["--repository", "luccas-silveira/knobler"])
            .out.trimmingCharacters(in: .whitespacesAndNewlines)
        if !repo.isEmpty, FileManager.default.fileExists(atPath: repo) {
            _ = run("/usr/bin/git", ["-C", repo, "pull", "--ff-only", "-q"])
        }
        let upgrade = run(brew, ["upgrade", "--cask", "knobler"],
                          env: ["HOMEBREW_NO_AUTO_UPDATE": "1"])
        guard upgrade.status == 0 else {
            fail(lastLine(upgrade.err.isEmpty ? upgrade.out : upgrade.err)); return false
        }
        return true
    }

    // MARK: caminho direto

    private func installDirect(_ release: Release) -> Bool {
        guard let asset = release.asset else {
            fail("Este release não tem .zip pra baixar."); return false
        }
        guard isInApplications else {
            fail("O Knobler não está em /Applications."); return false
        }
        let fm = FileManager.default
        let work = fm.temporaryDirectory.appendingPathComponent("knobler-update-\(release.version)")
        try? fm.removeItem(at: work)
        guard (try? fm.createDirectory(at: work, withIntermediateDirectories: true)) != nil else {
            fail("Não consegui criar a pasta temporária."); return false
        }
        defer { try? fm.removeItem(at: work) }

        // 1. baixa o zip (~7MB) de forma síncrona — já estamos fora da main queue
        let zip = work.appendingPathComponent("Knobler.zip")
        guard let data = try? Data(contentsOf: asset), (try? data.write(to: zip)) != nil else {
            fail("Falha ao baixar a atualização."); return false
        }

        // 2. extrai preservando os atributos do bundle
        let extracted = work.appendingPathComponent("extraido")
        let unzip = run("/usr/bin/ditto", ["-x", "-k", zip.path, extracted.path])
        guard unzip.status == 0 else {
            fail("Falha ao extrair: \(lastLine(unzip.err))"); return false
        }
        let newApp = extracted.appendingPathComponent("Knobler.app")
        guard fm.fileExists(atPath: newApp.path) else {
            fail("O zip não contém Knobler.app."); return false
        }

        // 3. sanidade antes de encostar em /Applications
        let verify = run("/usr/bin/codesign", ["--verify", "--strict", newApp.path])
        guard verify.status == 0 else {
            fail("Assinatura inválida no app baixado."); return false
        }
        let plist = newApp.appendingPathComponent("Contents/Info.plist")
        let downloaded = NSDictionary(contentsOf: plist)?["CFBundleShortVersionString"] as? String
        guard downloaded == release.version else {
            fail("O app baixado é \(downloaded ?? "?"), esperava \(release.version)."); return false
        }

        // 4. quarantine fora antes de mover (é o que o postflight do cask faz)
        _ = run("/usr/bin/xattr", ["-dr", "com.apple.quarantine", newApp.path])

        // 5. troca atômica; falha típica aqui é /Applications sem permissão
        do {
            _ = try fm.replaceItemAt(Bundle.main.bundleURL, withItemAt: newApp,
                                     backupItemName: nil, options: [.usingNewMetadataOnly])
        } catch {
            fail("Não consegui substituir o app: \(error.localizedDescription)")
            return false
        }
        return true
    }

    // MARK: relaunch

    /// O processo atual sobrevive à substituição do bundle (o inode antigo segue
    /// vivo), então quem reabre o app tem que ser alguém de fora: um shell que
    /// espera este PID morrer.
    private func relaunch() {
        let pid = ProcessInfo.processInfo.processIdentifier
        let path = Bundle.main.bundleURL.path
        let script = "while kill -0 \(pid) 2>/dev/null; do sleep 0.3; done; "
            + "sleep 1; open -a \"\(path)\""
        let p = Process()
        p.executableURL = URL(fileURLWithPath: "/bin/sh")
        p.arguments = ["-c", script]
        try? p.run()
        DispatchQueue.main.async { NSApp.terminate(nil) }
    }
}
```

- [ ] **Step 2: Compile**

```bash
xcodebuild -project Knobler.xcodeproj -scheme Knobler -configuration Debug build 2>&1 | tail -5
```

Esperado: `** BUILD SUCCEEDED **`.

- [ ] **Step 3: Verifique as sondas na mão**

Os dois comandos que decidem o caminho, rodados como o app roda:

```bash
/opt/homebrew/bin/brew list --cask knobler; echo "status=$?"
ls -ld /Applications/Knobler.app
```

Esperado nesta máquina: `brew list` falha (status ≠ 0) → o app vai pelo caminho direto; `/Applications/Knobler.app` existe → `isInApplications` verdadeiro.

- [ ] **Step 4: Ensaie o caminho direto sem o app**

Prove que a sequência baixa/extrai/verifica funciona antes de confiar nela em Swift:

```bash
cd "$(mktemp -d)" && \
curl -sL -o k.zip "$(curl -s https://api.github.com/repos/luccas-silveira/knobler/releases/latest | python3 -c 'import json,sys; print(json.load(sys.stdin)["assets"][0]["browser_download_url"])')" && \
/usr/bin/ditto -x -k k.zip out && \
/usr/bin/codesign --verify --strict out/Knobler.app && echo "ASSINATURA OK" && \
defaults read "$PWD/out/Knobler.app/Contents/Info.plist" CFBundleShortVersionString
```

Esperado: `ASSINATURA OK` e a versão do release. Se a assinatura falhar, pare: o
`installDirect` vai (corretamente) recusar instalar, e o problema é do artefato
publicado, não do código.

- [ ] **Step 5: Commit**

```bash
git add Knobler/Updater.swift
git commit -m "feat(updater): instala via brew ou download direto e reinicia"
```

---

### Task 4: Ajustes › Geral

O ponto de entrada permanente. Independe do card do notch — dá pra testar sozinho com `--ajustes=geral`.

**Files:**
- Modify: `Knobler/SettingsView.swift` (`GeneralSettingsPane`, a partir da linha 143)

**Interfaces:**
- Consumes: `Updater.shared`, `state`, `installedVersion`, `canInstall`, `releaseURL`, `check(force:)`, `install()`; `AppSettings.checkForUpdates`.
- Produces: nada consumido por outras tasks.

- [ ] **Step 1: Adicione a seção no fim do `Form` de `GeneralSettingsPane`**

Dentro de `GeneralSettingsPane`, acrescente a propriedade observada junto de `settings`:

```swift
    @ObservedObject var updater = Updater.shared
```

E, como última `Section` do `Form`:

```swift
            Section("Atualizações") {
                HStack {
                    VStack(alignment: .leading, spacing: 2) {
                        Text("Versão \(appVersion)")
                        Text(updateSubtitle)
                            .font(.caption)
                            .foregroundStyle(.secondary)
                    }
                    Spacer()
                    switch updater.state {
                    case .available:
                        if updater.canInstall {
                            Button("Atualizar") { updater.install() }
                                .buttonStyle(.borderedProminent)
                        } else if let url = updater.releaseURL {
                            Button("Ver release") { NSWorkspace.shared.open(url) }
                        }
                    case .installing:
                        ProgressView().controlSize(.small)
                    case .failed, .none:
                        Button("Verificar agora") { updater.check(force: true) }
                    }
                }
                SettingToggle(
                    title: "Verificar atualizações automaticamente",
                    subtitle: "Consulta o GitHub uma vez por dia e avisa no notch.",
                    isOn: $settings.checkForUpdates)
            }
```

E a propriedade que monta o texto secundário, ao lado de `appVersion`:

```swift
    private var updateSubtitle: String {
        switch updater.state {
        case .available(let release): return "Versão \(release.version) disponível"
        case .installing: return "Atualizando…"
        case .failed(let message): return message
        case .none: return "Você está na versão mais recente"
        }
    }
```

- [ ] **Step 2: Compile e abra o painel**

```bash
xcodebuild -project Knobler.xcodeproj -scheme Knobler -configuration Debug \
  -derivedDataPath build/dd build 2>&1 | tail -3
./build/dd/Build/Products/Debug/Knobler.app/Contents/MacOS/Knobler --ajustes=geral
```

Esperado: painel Geral abre com a seção "Atualizações", mostrando a versão e o
botão "Verificar agora". Clicar em "Verificar agora" numa build de dev
(`MARKETING_VERSION` = 0.0.0) faz aparecer "Versão X.Y.Z disponível" em alguns
segundos — é o teste de ponta a ponta da checagem.

- [ ] **Step 3: Commit**

```bash
git add Knobler/SettingsView.swift
git commit -m "feat(updater): seção de atualizações em Ajustes › Geral"
```

---

### Task 5: Card no notch

O aviso proativo. Aparece uma vez por versão e nunca por cima de um ditado.

**Files:**
- Modify: `Knobler/NotchViewModel.swift:41-107`
- Modify: `Knobler/NotchView.swift` (switch de conteúdo ~linha 128, `currentSize` ~linha 249, nova view ao lado de `airpodsConnectCard` ~linha 999)
- Modify: `tools/snapshot.sh` (lista de fontes), `tools/main.swift` (cenário)

**Interfaces:**
- Consumes: `UpdateState`, `Release` da Task 1.
- Produces:
  - `NotchViewModel.update: UpdateState?`
  - `NotchViewModel.updateCard: Bool`
  - `NotchViewModel.onUpdateInstall: (() -> Void)?`
  - `NotchViewModel.onUpdateSkip: (() -> Void)?`
  - `NotchViewModel.Mode.update`

- [ ] **Step 1: Estado no view model**

Em `Knobler/NotchViewModel.swift`, junto dos outros `@Published` (depois de `airpodsCard`, linha 51):

```swift
    /// Atualização disponível/instalando. Espelha o Updater; quem empurra é o
    /// AppDelegate, como faz com AirPods e Pomodoro.
    @Published var update: UpdateState?
    /// Card de update em exibição. Sem esta flag o card ficaria permanente: o
    /// `update` não é transitório como um HUD.
    @Published var updateCard = false
    /// Dá pra instalar de dentro do app? false = sem brew e sem asset baixável,
    /// e o botão do card vira "Ver release" em vez de mentir.
    @Published var updateCanInstall = true
    /// Ações do card — o app conecta no Updater.
    var onUpdateInstall: (() -> Void)?
    var onUpdateSkip: (() -> Void)?
```

- [ ] **Step 2: Modo e prioridade**

No `enum Mode` (linha 92), acrescente `update`:

```swift
    enum Mode: Equatable {
        case closed, music, notification, hud, dictation, question, pomodoro, airpods, message, update
    }
```

E em `var mode` (linha 98), entre `hud` e `airpodsCard` — update nunca interrompe
ditado, notificação ou HUD:

```swift
        if hud != nil { return .hud }
        if updateCard, update != nil { return .update }
        if airpodsCard { return .airpods }
```

Atualize também o comentário de prioridade logo acima (linha 95) para citar update
entre HUD e AirPods.

- [ ] **Step 3: Tamanho do card**

Em `Knobler/NotchView.swift`, dentro de `currentSize` (linha 201), ao lado de `case .airpods`:

```swift
        case .update:
            return CGSize(width: 380, height: topInset + 96)
```

- [ ] **Step 4: A view**

Em `Knobler/NotchView.swift`, no switch de conteúdo, depois do `case .airpods` (linha 128-134):

```swift
            case .update:
                updateNotchCard
                    .frame(width: 380 - 40)
                    .padding(.top, topInset)
                    .padding(.bottom, 12)
                    // desce do notch, como as notificações
                    .transition(.blurReplace.combined(with: .move(edge: .top)))
```

E a view, ao lado de `airpodsConnectCard` (linha 999):

```swift
    /// Nome com sufixo `NotchCard` de propósito: `updateCard` já é a flag de
    /// exibição no view model, e as duas coisas convivem no mesmo escopo aqui.
    @ViewBuilder
    private var updateNotchCard: some View {
        if let state = vm.update {
            HStack(spacing: 12) {
                Image(systemName: "arrow.down.circle.fill")
                    .font(.title2)
                    .foregroundStyle(.white)
                    .frame(width: 30)
                VStack(alignment: .leading, spacing: 4) {
                    switch state {
                    case .available(let release):
                        Text("Knobler \(release.version) disponível")
                            .font(.subheadline.weight(.semibold))
                            .foregroundStyle(.white)
                        Text(release.notes.isEmpty ? "Nova versão pronta pra instalar."
                                                   : release.notes)
                            .font(.caption)
                            .foregroundStyle(.white.opacity(0.7))
                            .lineLimit(2)
                    case .installing:
                        Text("Atualizando…")
                            .font(.subheadline.weight(.semibold))
                            .foregroundStyle(.white)
                        Text("O Knobler reinicia sozinho ao terminar.")
                            .font(.caption)
                            .foregroundStyle(.white.opacity(0.7))
                    case .failed(let message):
                        Text("Falha ao atualizar")
                            .font(.subheadline.weight(.semibold))
                            .foregroundStyle(.white)
                        Text(message)
                            .font(.caption)
                            .foregroundStyle(.white.opacity(0.7))
                            .lineLimit(2)
                    }
                }
                Spacer(minLength: 0)
                if case .installing = state {
                    ProgressView().controlSize(.small).tint(.white)
                } else {
                    HStack(spacing: 6) {
                        Button("Depois") { vm.onUpdateSkip?() }
                            .buttonStyle(.plain)
                            .font(.caption)
                            .foregroundStyle(.white.opacity(0.6))
                        Button(vm.updateCanInstall ? "Atualizar" : "Ver release") {
                            vm.onUpdateInstall?()
                        }
                            .buttonStyle(.borderedProminent)
                            .controlSize(.small)
                    }
                }
            }
            .padding(.horizontal, 4)
        }
    }
```

- [ ] **Step 5: Cenário de snapshot**

Em `tools/snapshot.sh`, acrescente à lista de fontes (junto de `Knobler/AirPodsBattery.swift`):

```
  Knobler/Updater.swift \
```

Em `tools/main.swift`, no fim do array `scenarios` (depois do cenário `airpods-low`):

```swift
    // Update: card de versão nova e o estado de instalação.
    Scenario(name: "update-card", realNotch: true) { vm, _, _ in
        vm.update = .available(Release(
            version: "0.9.0",
            notes: "Fixed\nDitado não cai mais a cada release",
            url: URL(string: "https://github.com/luccas-silveira/knobler/releases/tag/v0.9.0")!,
            asset: nil))
        vm.updateCard = true
    },
    Scenario(name: "update-installing", realNotch: true) { vm, _, _ in
        vm.update = .installing
        vm.updateCard = true
    },
```

- [ ] **Step 6: Renderize e olhe**

```bash
./tools/snapshot.sh
```

Esperado: build sem erro e `Snapshots/update-card.png` + `Snapshots/update-installing.png`
gerados. **Abra os dois PNGs e confira**: título, notas em duas linhas no máximo,
os botões visíveis dentro da largura, nada cortado. O ícone "proibido" no lugar do
card significaria uma subview que precisa de `NSView` real — não é o caso aqui
(tudo é SwiftUI puro), mas se aparecer, o culpado é o `ProgressView`; troque por
um `Text("…")` no cenário.

- [ ] **Step 7: Compile o app**

```bash
xcodebuild -project Knobler.xcodeproj -scheme Knobler -configuration Debug build 2>&1 | tail -3
```

Esperado: `** BUILD SUCCEEDED **`.

- [ ] **Step 8: Commit**

```bash
git add Knobler/NotchViewModel.swift Knobler/NotchView.swift tools/snapshot.sh tools/main.swift
git commit -m "feat(updater): card de atualização no notch"
```

---

### Task 6: Fiação e documentação

Liga o `Updater` ao app: preferência, propagação pros notches e ações do card. Sem isso nada acontece sozinho.

**Files:**
- Modify: `Knobler/KnoblerApp.swift` (`applicationDidFinishLaunching`, ~linha 99; bloco de reação a `AppSettings.objectWillChange`, ~linha 381)
- Modify: `CHANGELOG.md`, `docs/settings.md:23`, `README.md`

**Interfaces:**
- Consumes: tudo das Tasks 1-5.
- Produces: nada.

- [ ] **Step 1: Fie o Updater no AppDelegate**

Em `applicationDidFinishLaunching`, junto dos outros observadores (perto da linha 174, onde AirPods é propagado), acrescente:

```swift
        // Atualizações: o Updater publica, o notch reflete. O card só abre uma
        // vez por versão (skipCurrent grava a dispensada).
        Updater.shared.automatic = AppSettings.shared.checkForUpdates
        updaterCancellable = Updater.shared.$state
            .receive(on: DispatchQueue.main)
            .sink { [weak self] state in
                guard let self else { return }
                self.notches.values.forEach { notch in
                    notch.viewModel.update = state
                    notch.viewModel.updateCanInstall = Updater.shared.canInstall
                    // "Depois" some do notch mas continua nos Ajustes; instalando,
                    // o card fica de pé pra mostrar o progresso.
                    switch state {
                    case .available:
                        notch.viewModel.updateCard = !Updater.shared.isSkipped
                    case .installing, .failed:
                        notch.viewModel.updateCard = true
                    case .none:
                        notch.viewModel.updateCard = false
                    }
                }
            }
        Updater.shared.start()
```

Declare o cancellable junto dos outros campos do `AppDelegate`:

```swift
    private var updaterCancellable: AnyCancellable?
```

- [ ] **Step 2: Conecte as ações do card**

Onde os view models são criados/configurados (o mesmo ponto em que `onSendReply` é
ligado), acrescente para cada view model:

```swift
        vm.onUpdateInstall = {
            // Sem brew e sem asset instalável, o botão diz "Ver release" — e é
            // isso que ele tem que fazer.
            if Updater.shared.canInstall {
                Updater.shared.install()
            } else if let url = Updater.shared.releaseURL {
                NSWorkspace.shared.open(url)
            }
        }
        vm.onUpdateSkip = {
            Updater.shared.skipCurrent()
            vm.updateCard = false
        }
```

Se a criação dos notches for centralizada numa função, ligue lá — as duas closures
precisam existir em **todo** view model, senão os botões do card não fazem nada no
monitor externo.

- [ ] **Step 3: Espelhe o toggle de preferência**

No bloco que reage a `AppSettings.shared.objectWillChange` (linha ~381, junto de
`if AppSettings.shared.airpodsNotch { ... }`):

```swift
                    Updater.shared.automatic = AppSettings.shared.checkForUpdates
```

- [ ] **Step 4: Compile e rode o app de verdade**

```bash
xcodebuild -project Knobler.xcodeproj -scheme Knobler -configuration Debug \
  -derivedDataPath build/dd build 2>&1 | tail -3
./build/dd/Build/Products/Debug/Knobler.app/Contents/MacOS/Knobler
```

Esperado: build Debug tem `MARKETING_VERSION` = 0.0.0, então **qualquer** release
publicado conta como novidade. Em ~30s o card deve descer do notch anunciando a
versão mais recente. Teste os dois botões:

- `Depois` → o card some e não volta; a seção em `--ajustes=geral` continua
  mostrando "disponível".
- `Atualizar` → o card vira "Atualizando…". Nesta máquina isso segue o caminho
  direto e **vai substituir `/Applications/Knobler.app`** pela versão publicada.
  Se você não quer isso agora, teste só o `Depois` e deixe o `Atualizar` para
  quando a próxima release sair de verdade.

Mate o app com `killall Knobler` ao terminar.

- [ ] **Step 5: Documente**

Em `CHANGELOG.md`, dentro de `## [Unreleased]` (crie a seção `### Added` se não existir):

```markdown
### Added
- **Aviso de atualização**: o Knobler consulta o GitHub uma vez por dia, mostra um
  card no notch quando sai versão nova e instala com um clique — via `brew upgrade`
  se o app veio do cask, ou baixando o `.zip` do release se não. Em Ajustes › Geral
  a versão instalada, o botão de atualizar e o toggle da checagem automática.
```

Em `docs/settings.md:23`, a linha do painel Geral hoje diz "login automático, versão
do app". Estenda para citar as atualizações:

```markdown
  - **Geral** — login automático, versão do app, atualizações.
```

E acrescente ao corpo do documento, na descrição do painel Geral, um parágrafo:
o Knobler consulta o GitHub uma vez por dia, avisa no notch quando sai versão nova
(uma vez por versão — "Depois" silencia até a próxima), e o botão **Atualizar**
instala na hora e reinicia o app. Quem instalou pelo Homebrew recebe o update pelo
próprio `brew upgrade`; quem baixou o `.zip` recebe o download direto. A checagem
automática pode ser desligada no mesmo painel.

No `README.md`, na seção de instalação/atualização, registre que o app agora avisa
sozinho e que `brew upgrade --cask knobler` continua funcionando na mão.

> A imagem `docs/images/settings-geral.png` fica desatualizada com a seção nova.
> Regerar é manual (o `snapshot.sh` não cobre Ajustes): rode
> `Knobler.app/Contents/MacOS/Knobler --ajustes=geral`, capture a janela com
> `screencapture -l<windowID>`, corte pra `802x554+55+37` e salve por cima.

- [ ] **Step 6: Rode todos os checks**

```bash
xcrun swiftc -parse-as-library -swift-version 5 \
  Knobler/Updater.swift tools/updatercheck.swift -o /tmp/updatercheck && /tmp/updatercheck
./tools/snapshot.sh
xcodebuild -project Knobler.xcodeproj -scheme Knobler -configuration Debug build 2>&1 | tail -3
```

Esperado: `✅ updatercheck ok`, snapshots regenerados sem erro, `** BUILD SUCCEEDED **`.

- [ ] **Step 7: Commit**

```bash
git add Knobler/KnoblerApp.swift CHANGELOG.md README.md docs/
git commit -m "feat(updater): fia o updater no app e documenta"
```

---

## Depois do plano

O ciclo completo (brew/download → substituir o bundle → relaunch) só se prova numa
release de verdade. Publique a próxima versão normalmente com
`./tools/release.sh minor` e atualize a partir da anterior — é o teste de aceitação.

Lembrete do spec: nesta máquina o primeiro update pelo caminho direto troca a
identidade de assinatura (*Apple Development* → *Knobler Local Signing*) e vai
derrubar a permissão de Acessibilidade **uma vez**. Reconceda em Ajustes do Sistema
→ Privacidade e Segurança → Acessibilidade; das próximas vezes não acontece mais.
