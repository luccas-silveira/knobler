# Card aberto com foco único — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Trocar a pilha de seções do notch expandido por uma lista ordenada + um índice de foco, com ordem-base configurável promovida por eventos de transição recentes.

**Architecture:** Uma função pura (`NotchSectionOrder.ordenar`) decide a ordem; o `NotchViewModel` guarda o foco e a altura resultante; a `NotchView` desenha só a seção em foco e uma faixa de ícones no rodapé. O monitor de scroll passa a ler a altura publicada em vez de constantes literais.

**Tech Stack:** Swift 5, SwiftUI + AppKit, macOS 14.2 como deployment target. Projeto gerado por XcodeGen. Testes são binários `tools/*check.swift` com `assert`, rodados por `tools/check.sh` — **não existe XCTest neste repo**.

**Spec:** `docs/specs/card-foco.md`. Viabilidade: `docs/specs/card-foco-pesquisa.md`.

## Global Constraints

- Comentários e strings de UI em **pt-BR**.
- Deployment target **macOS 14.2**. Nada de `glassEffect`/Liquid Glass sem `if #available(macOS 26, *)`.
- Marcar simplificações deliberadas com `// ponytail:`.
- **Nunca** editar `Knobler.xcodeproj` à mão. Arquivo novo em `Knobler/` → rodar `xcodegen generate`.
- **Nunca** editar `MARKETING_VERSION` nem criar tag manual. Mudanças vão pra `## [Unreleased]` do `CHANGELOG.md`.
- Arquivo novo em `Knobler/` que a `NotchView` use → adicionar também na lista manual de `tools/snapshot.sh`.
- Build de verificação: `xcodebuild -project Knobler.xcodeproj -scheme Knobler -configuration Debug build`.
- Gate completo: `./tools/check.sh` (16 checks hoje).

## File Structure

| Arquivo | Responsabilidade |
|---|---|
| `Knobler/NotchSectionOrder.swift` **(novo)** | Enum `NotchSection`, struct `NotchSectionState`, a função pura de ordenação e o saneamento da ordem salva. Sem `import AppKit`/`SwiftUI` — é o que o torna testável fora do app, igual `NotchGesture.swift`. |
| `tools/sectionordercheck.swift` **(novo)** | Self-check com `assert`, no molde de `tools/historycheck.swift`. |
| `Knobler/NotchViewModel.swift` | Ganha `focus`, `focusLocked`, `cardHeight`, os `updatedAt` por seção e o método que recalcula a ordem na abertura. |
| `Knobler/NotchView.swift` | `expandedContent` vira `switch focus`; nasce `sectionStrip`; morrem `musicContent`, `pageDots`, `grabber`; `currentSize` do modo `.music` encolhe. |
| `Knobler/KnoblerApp.swift` | `handleScroll` lê `vm.cardHeight`; horizontal percorre a faixa; some o alvo `.history`. |
| `Knobler/NotchGesture.swift` | `ScrollTarget` perde `.history`. |
| `Knobler/AppSettings.swift` | Persiste `notchSectionOrder: [String]`. |
| `Knobler/SettingsView.swift` | Lista drag-to-reorder no painel Notch. |
| `Knobler/NotchAPIServer.swift` | `GET /status` passa a incluir a seção em foco. |

---

### Task 1: A função pura de ordenação

**Files:**
- Create: `Knobler/NotchSectionOrder.swift`
- Create: `tools/sectionordercheck.swift`
- Modify: `tools/check.sh` (linha do bloco `== self-checks Swift ==`)
- Modify: `tools/snapshot.sh` (lista de arquivos)

**Interfaces:**
- Produces:
  - `enum NotchSection: String, CaseIterable` com casos `musica, atividade, pomodoro, shelf, espelho, mensagens, historico, nota`
  - `struct NotchSectionState { let section: NotchSection; let hasContent: Bool; let lastEvent: Date? }`
  - `NotchSectionOrder.padrao: [NotchSection]`
  - `NotchSectionOrder.janelaDePromocao: TimeInterval`
  - `NotchSectionOrder.ordenar(base:estados:agora:travadaNaNota:) -> [NotchSection]`
  - `NotchSectionOrder.sanear(salva:) -> [NotchSection]`

- [ ] **Step 1: Escrever o self-check que falha**

Criar `tools/sectionordercheck.swift`:

```swift
//
//  tools/sectionordercheck.swift — self-check da ordenação das seções do card.
//  NÃO faz parte do alvo do app.
//
//  Rodar:
//  xcrun swiftc -parse-as-library -swift-version 5 \
//    Knobler/NotchSectionOrder.swift tools/sectionordercheck.swift \
//    -o /tmp/sectionordercheck && /tmp/sectionordercheck
//

import Foundation

@main
struct SectionOrderCheck {
    static func main() {
        testOrdemBaseEmRepouso()
        testEventoRecenteSobe()
        testEventoAntigoNaoSobe()
        testSemConteudoSai()
        testMaisRecentePrimeiro()
        testNotaTravaTudo()
        testSanearDescartaDesconhecida()
        testSanearCompletaFaltantes()
        print("✅ sectionordercheck ok")
    }

    static let agora = Date(timeIntervalSince1970: 1_000_000)

    /// Atalho: seção com conteúdo cujo último evento foi há `haSegundos`.
    static func viva(_ s: NotchSection, haSegundos: TimeInterval?) -> NotchSectionState {
        NotchSectionState(section: s, hasContent: true,
                          lastEvent: haSegundos.map { agora.addingTimeInterval(-$0) })
    }

    /// Sem evento nenhum, manda a ordem que o usuário escolheu.
    static func testOrdemBaseEmRepouso() {
        let base: [NotchSection] = [.shelf, .musica, .atividade]
        let out = NotchSectionOrder.ordenar(
            base: base,
            estados: [viva(.musica, haSegundos: nil),
                      viva(.shelf, haSegundos: nil),
                      viva(.atividade, haSegundos: nil)],
            agora: agora, travadaNaNota: false)
        assert(out == [.shelf, .musica, .atividade], "ordem-base não respeitada: \(out)")
    }

    /// Evento dentro da janela sobe pro topo, à frente da ordem-base.
    static func testEventoRecenteSobe() {
        let out = NotchSectionOrder.ordenar(
            base: [.shelf, .musica, .atividade],
            estados: [viva(.musica, haSegundos: nil),
                      viva(.shelf, haSegundos: nil),
                      viva(.atividade, haSegundos: 3)],
            agora: agora, travadaNaNota: false)
        assert(out == [.atividade, .shelf, .musica], "evento de 3 s não promoveu: \(out)")
    }

    /// Fora da janela não promove — é o que impede o tique do Pomodoro e a
    /// posição da música de morarem no topo pra sempre.
    static func testEventoAntigoNaoSobe() {
        let out = NotchSectionOrder.ordenar(
            base: [.shelf, .musica, .atividade],
            estados: [viva(.musica, haSegundos: nil),
                      viva(.shelf, haSegundos: nil),
                      viva(.atividade, haSegundos: 30)],
            agora: agora, travadaNaNota: false)
        assert(out == [.shelf, .musica, .atividade], "evento de 30 s promoveu: \(out)")
    }

    /// Seção sem conteúdo some da lista inteira — nem como ícone aparece.
    static func testSemConteudoSai() {
        let out = NotchSectionOrder.ordenar(
            base: [.shelf, .musica, .atividade],
            estados: [viva(.musica, haSegundos: nil),
                      NotchSectionState(section: .shelf, hasContent: false, lastEvent: nil),
                      viva(.atividade, haSegundos: nil)],
            agora: agora, travadaNaNota: false)
        assert(out == [.musica, .atividade], "seção vazia sobreviveu: \(out)")
    }

    /// Duas promovidas: a mais recente vem primeiro.
    static func testMaisRecentePrimeiro() {
        let out = NotchSectionOrder.ordenar(
            base: [.musica, .atividade, .shelf],
            estados: [viva(.musica, haSegundos: nil),
                      viva(.atividade, haSegundos: 8),
                      viva(.shelf, haSegundos: 2)],
            agora: agora, travadaNaNota: false)
        assert(out == [.shelf, .atividade, .musica], "recência entre promovidas errada: \(out)")
    }

    /// Digitando na nota, ela vence qualquer promoção. Sem isso o foco sai do
    /// campo, o `.onDisappear` zera o foco de teclado e as teclas seguintes
    /// vazam pro app da frente sem sinal nenhum.
    static func testNotaTravaTudo() {
        let out = NotchSectionOrder.ordenar(
            base: [.musica, .atividade, .nota],
            estados: [viva(.musica, haSegundos: nil),
                      viva(.atividade, haSegundos: 1),
                      viva(.nota, haSegundos: 600)],
            agora: agora, travadaNaNota: true)
        assert(out.first == .nota, "nota não travou o foco: \(out)")
    }

    /// Chave do UserDefaults com lixo (versão antiga, seção removida) não
    /// pode derrubar a lista.
    static func testSanearDescartaDesconhecida() {
        let out = NotchSectionOrder.sanear(salva: ["musica", "fantasma", "shelf"])
        assert(!out.contains(where: { $0.rawValue == "fantasma" }), "seção desconhecida passou")
        assert(out.count == NotchSection.allCases.count, "sanear perdeu seções: \(out)")
    }

    /// Ordem salva de uma versão antiga (sem as seções novas) ganha o resto no
    /// fim, na ordem padrão.
    static func testSanearCompletaFaltantes() {
        let out = NotchSectionOrder.sanear(salva: ["shelf", "musica"])
        assert(Array(out.prefix(2)) == [.shelf, .musica], "prefixo salvo não preservado: \(out)")
        assert(Set(out) == Set(NotchSection.allCases), "sanear não completou: \(out)")
    }
}
```

- [ ] **Step 2: Rodar e ver falhar**

Run:
```bash
xcrun swiftc -parse-as-library -swift-version 5 \
  Knobler/NotchSectionOrder.swift tools/sectionordercheck.swift \
  -o /tmp/sectionordercheck && /tmp/sectionordercheck
```
Expected: FALHA — `error: no such file or directory: 'Knobler/NotchSectionOrder.swift'`.

- [ ] **Step 3: Implementar o módulo puro**

Criar `Knobler/NotchSectionOrder.swift`:

```swift
//
//  NotchSectionOrder.swift
//  Knobler
//
//  A parte pura da hierarquia do card aberto: quem aparece e em que ordem.
//  Sem AppKit/SwiftUI de propósito — é o que permite rodar isto no
//  sectionordercheck sem subir o app, como o NotchGesture faz com o scroll.
//

import Foundation

/// Uma seção do card expandido. O `rawValue` é o que vai pro UserDefaults e
/// pro `GET /status`, então renomear um caso quebra ordem salva de usuário.
enum NotchSection: String, CaseIterable {
    case musica, atividade, pomodoro, shelf, espelho, mensagens, historico, nota
}

/// O que o VM sabe de uma seção no instante em que o card abre.
struct NotchSectionState: Equatable {
    let section: NotchSection
    /// Tem o que mostrar? Sem música tocando, shelf vazio, Pomodoro idle → false.
    let hasContent: Bool
    /// Último **evento de transição** (troca de faixa, virada de fase, item
    /// novo). Nunca é o tique de progresso: `remaining` do Pomodoro e a posição
    /// da música mudam a cada segundo e promoveriam essas duas pra sempre.
    let lastEvent: Date?
}

enum NotchSectionOrder {
    /// Ordem de fábrica, usada quando não há nada salvo nos Ajustes.
    static let padrao: [NotchSection] = [
        .musica, .atividade, .pomodoro, .shelf, .espelho, .mensagens, .historico, .nota,
    ]

    /// Quanto tempo um evento continua promovendo a seção dele.
    ///
    /// ponytail: janela fixa em vez de preferência. Vira ajuste se alguém
    /// reclamar; o card vive segundos e ninguém cronometra isso.
    static let janelaDePromocao: TimeInterval = 10

    /// Ordem efetiva do card. Calculada UMA vez, na abertura — o congelamento
    /// é de quem chama (o VM), não daqui.
    ///
    /// - Parameters:
    ///   - base: ordem escolhida nos Ajustes.
    ///   - estados: o que cada seção tem a dizer agora.
    ///   - agora: injetado pra que o teste não dependa do relógio.
    ///   - travadaNaNota: usuário digitando na nota nesta tela.
    static func ordenar(base: [NotchSection],
                        estados: [NotchSectionState],
                        agora: Date,
                        travadaNaNota: Bool) -> [NotchSection] {
        let porSecao = Dictionary(uniqueKeysWithValues: estados.map { ($0.section, $0) })
        // seções fora da `base` (versão salva antiga) entram no fim, senão
        // sumiriam da UI sem ninguém perceber
        let ordemBase = base + padrao.filter { !base.contains($0) }
        let visiveis = ordemBase.filter { porSecao[$0]?.hasContent == true }

        let promovidas = visiveis
            .filter { s in
                guard let quando = porSecao[s]?.lastEvent else { return false }
                return agora.timeIntervalSince(quando) < janelaDePromocao
            }
            // mais recente primeiro; empate cai na ordem-base (sort estável não
            // é garantido em Swift, então o índice entra no critério)
            .sorted { a, b in
                let ta = porSecao[a]?.lastEvent ?? .distantPast
                let tb = porSecao[b]?.lastEvent ?? .distantPast
                if ta != tb { return ta > tb }
                return ordemBase.firstIndex(of: a)! < ordemBase.firstIndex(of: b)!
            }

        let resto = visiveis.filter { !promovidas.contains($0) }
        let ordenadas = promovidas + resto

        // digitar é o compromisso mais forte com o notch: nada tira o foco do
        // campo enquanto o teclado está nos dedos.
        guard travadaNaNota, ordenadas.contains(.nota) else { return ordenadas }
        return [.nota] + ordenadas.filter { $0 != .nota }
    }

    /// Lê a ordem do UserDefaults sem confiar nela: descarta o que não existe
    /// mais e completa o que faltou (versão antiga não conhece seção nova).
    static func sanear(salva: [String]) -> [NotchSection] {
        var vistas: [NotchSection] = []
        for raw in salva {
            guard let s = NotchSection(rawValue: raw), !vistas.contains(s) else { continue }
            vistas.append(s)
        }
        return vistas + padrao.filter { !vistas.contains($0) }
    }
}
```

- [ ] **Step 4: Rodar e ver passar**

Run:
```bash
xcrun swiftc -parse-as-library -swift-version 5 \
  Knobler/NotchSectionOrder.swift tools/sectionordercheck.swift \
  -o /tmp/sectionordercheck && /tmp/sectionordercheck
```
Expected: `✅ sectionordercheck ok`

- [ ] **Step 5: Registrar no gate e no harness**

Em `tools/check.sh`, logo depois da linha do `historycheck`:

```bash
swift_check sectionordercheck    Knobler/NotchSectionOrder.swift tools/sectionordercheck.swift
```

Em `tools/snapshot.sh`, acrescentar à lista de fontes (logo após `Knobler/NotchGesture.swift \`):

```
  Knobler/NotchSectionOrder.swift \
```

- [ ] **Step 6: Gerar o projeto e rodar o gate inteiro**

Run:
```bash
xcodegen generate
./tools/check.sh
```
Expected: `✅ 17 checks ok`

- [ ] **Step 7: Commit**

```bash
git add Knobler/NotchSectionOrder.swift tools/sectionordercheck.swift tools/check.sh tools/snapshot.sh Knobler.xcodeproj
git commit -m "feat: ordenação pura das seções do card aberto

Ordem-base do usuário promovida por eventos de transição recentes, com a
nota travando o foco enquanto o teclado está nos dedos. Sem UI ainda —
só a função e o self-check."
```

---

### Task 2: Carimbar os eventos de transição no VM

**Files:**
- Modify: `Knobler/NotchViewModel.swift`

**Interfaces:**
- Consumes: `NotchSection`, `NotchSectionState` (Task 1)
- Produces:
  - `NotchViewModel.marcarEvento(_ section: NotchSection)`
  - `NotchViewModel.eventos: [NotchSection: Date]` (leitura interna)
  - `NotchViewModel.estadoDasSecoes(hasMusic:hasShelf:hasHistory:hasMensagens:) -> [NotchSectionState]`

A distinção evento × tique é o coração desta task. Tabela normativa (do spec):

| Seção | É evento | **Não** é evento |
|---|---|---|
| musica | trocou a faixa; `isPlaying` virou | avanço da posição |
| pomodoro | mudou `phase` ou `runState` | tique do `remaining` |
| atividade | mudou `title`/`detail`; cruzou `done` | cada passo do `progress` |
| shelf | `items.count` mudou | — |
| espelho | `mirrorOn` virou | — |
| mensagens | mensagem nova | — |
| historico | notificação nova | — |
| nota | rascunho passou de vazio a não-vazio | cada tecla |

- [ ] **Step 1: Adicionar o registro de eventos**

Em `Knobler/NotchViewModel.swift`, dentro da classe, perto dos outros `@Published`:

```swift
    /// Quando cada seção teve o último **evento de transição**. É o insumo da
    /// promoção; ver a tabela em docs/specs/card-foco.md pra o que conta.
    /// Não é `@Published`: só é lido no instante em que o card abre.
    private(set) var eventos: [NotchSection: Date] = [:]

    func marcarEvento(_ section: NotchSection, at date: Date = Date()) {
        eventos[section] = date
    }
```

- [ ] **Step 2: Carimbar nas transições que o próprio VM já vê**

Ainda em `NotchViewModel.swift`, trocar as declarações existentes por versões com `didSet`:

```swift
    @Published var mirrorOn = false {
        didSet { if mirrorOn != oldValue { marcarEvento(.espelho) } }
    }

    /// Timer Pomodoro em exibição (pílula própria no notch). nil = idle.
    /// Só fase/estado promovem — `remaining` muda a cada segundo e faria o
    /// Pomodoro morar no topo pra sempre.
    @Published var pomodoro: PomodoroState? {
        didSet {
            guard pomodoro?.phase != oldValue?.phase
                || pomodoro?.runState != oldValue?.runState else { return }
            marcarEvento(.pomodoro)
        }
    }

    @Published var activity: NotchActivity? {
        didSet {
            guard activity?.title != oldValue?.title
                || activity?.detail != oldValue?.detail
                || (activity == nil) != (oldValue == nil) else { return }
            marcarEvento(.atividade)
        }
    }
```

- [ ] **Step 3: Expor o estado das seções**

Acrescentar ao `NotchViewModel`:

```swift
    /// Retrato das seções pro `NotchSectionOrder`. Os dados que não moram no
    /// VM (música, shelf, histórico, mensagens) entram por parâmetro: quem
    /// chama é a NotchView, que já observa esses stores.
    func estadoDasSecoes(hasMusic: Bool, hasShelf: Bool,
                         hasHistory: Bool, hasMensagens: Bool,
                         hasNota: Bool) -> [NotchSectionState] {
        let conteudo: [NotchSection: Bool] = [
            .musica: hasMusic,
            .atividade: activity != nil,
            .pomodoro: pomodoro != nil,
            .shelf: hasShelf,
            .espelho: mirrorOn,
            .mensagens: hasMensagens,
            .historico: hasHistory,
            .nota: hasNota,
        ]
        return NotchSection.allCases.map {
            NotchSectionState(section: $0,
                              hasContent: conteudo[$0] ?? false,
                              lastEvent: eventos[$0])
        }
    }
```

- [ ] **Step 4: Carimbar os eventos que nascem fora do VM**

Em `Knobler/NotchView.swift`, dentro do `body` do `interactiveNotch` (junto dos outros `.onChange`), acrescentar:

```swift
        // música: só troca de faixa e play/pause promovem — a posição avança
        // continuamente e promoveria a música pra sempre
        .onChange(of: media.state?.title) { _, _ in vm.marcarEvento(.musica) }
        .onChange(of: media.state?.isPlaying) { _, _ in vm.marcarEvento(.musica) }
        .onChange(of: shelf.items.count) { _, _ in vm.marcarEvento(.shelf) }
        .onChange(of: history.items.count) { _, _ in vm.marcarEvento(.historico) }
        .onChange(of: note.text.isEmpty) { _, vazio in
            if !vazio { vm.marcarEvento(.nota) }
        }
```

E em `Knobler/KnoblerApp.swift`, no ponto onde a mensagem LAN é entregue ao VM (o mesmo trecho que hoje seta `vm.incoming`), acrescentar na mesma linha de tempo:

```swift
            vm.marcarEvento(.mensagens)
```

- [ ] **Step 5: Compilar**

Run: `xcodebuild -project Knobler.xcodeproj -scheme Knobler -configuration Debug build 2>&1 | tail -3`
Expected: `** BUILD SUCCEEDED **`

- [ ] **Step 6: Commit**

```bash
git add Knobler/NotchViewModel.swift Knobler/NotchView.swift Knobler/KnoblerApp.swift
git commit -m "feat: carimbo de eventos de transição por seção

Só transição promove: troca de faixa, virada de fase, item novo. Tique de
remaining e avanço de posição ficam de fora — senão Pomodoro e música
morariam no topo pra sempre."
```

---

### Task 3: Foco e altura no VM

**Files:**
- Modify: `Knobler/NotchViewModel.swift`

**Interfaces:**
- Consumes: `NotchSectionOrder.ordenar`, `estadoDasSecoes` (Tasks 1–2)
- Produces:
  - `@Published private(set) var secoes: [NotchSection]`
  - `@Published var focus: NotchSection?`
  - `@Published private(set) var focusLocked: Bool`
  - `@Published private(set) var cardHeight: CGFloat`
  - `func recalcularSecoes(_ estados: [NotchSectionState], travadaNaNota: Bool)`
  - `func focar(_ section: NotchSection)`
  - `func focarVizinho(avancando: Bool)`
  - `func publicarAltura(_ h: CGFloat)`

- [ ] **Step 1: Adicionar o estado**

Em `Knobler/NotchViewModel.swift`:

```swift
    /// Ordem efetiva do card, congelada no instante da abertura. Recalcular a
    /// cada mudança faria o conteúdo pular debaixo do cursor e mover o alvo de
    /// clique da faixa.
    ///
    /// `internal` e não `private(set)`: o harness de snapshot monta a lista à
    /// mão pra capturar cenários que não dá pra provocar offscreen.
    @Published var secoes: [NotchSection] = []
    @Published var focus: NotchSection?
    /// Escolha manual do usuário (clique na faixa ou swipe): nenhuma promoção
    /// tira o foco até o notch recolher.
    @Published private(set) var focusLocked = false
    /// Altura do card aberto agora. Publicada porque o monitor de scroll roda
    /// fora do SwiftUI e precisa dela pra delimitar a zona do gesto.
    @Published private(set) var cardHeight: CGFloat = 0

    func recalcularSecoes(_ estados: [NotchSectionState], travadaNaNota: Bool) {
        secoes = NotchSectionOrder.ordenar(base: AppSettings.shared.notchSectionOrder,
                                           estados: estados,
                                           agora: Date(),
                                           travadaNaNota: travadaNaNota)
        // a trava da nota vence até a escolha manual anterior
        if travadaNaNota, secoes.contains(.nota) {
            focus = .nota
            return
        }
        guard !focusLocked, let primeira = secoes.first else {
            // o foco travado pode ter perdido o conteúdo enquanto isso
            if let f = focus, !secoes.contains(f) { focus = secoes.first; focusLocked = false }
            return
        }
        focus = primeira
    }

    func focar(_ section: NotchSection) {
        guard secoes.contains(section) else { return }
        focus = section
        focusLocked = true
    }

    /// Swipe horizontal no card: anda um passo na faixa e trava, igual ao clique.
    func focarVizinho(avancando: Bool) {
        guard let atual = focus, let i = secoes.firstIndex(of: atual), secoes.count > 1 else { return }
        let destino = (i + (avancando ? 1 : -1) + secoes.count) % secoes.count
        focar(secoes[destino])
    }

    func publicarAltura(_ h: CGFloat) {
        guard abs(h - cardHeight) > 0.5 else { return }
        cardHeight = h
    }
```

- [ ] **Step 2: Soltar a trava ao recolher**

No `didSet` de `expanded` (que hoje só desliga o espelho), acrescentar:

```swift
    @Published var expanded = false {
        // recolher o notch desliga o espelho — a câmera nunca fica ligada escondida
        didSet {
            if !expanded {
                mirrorOn = false
                // escolha manual vale até fechar; a próxima abertura volta ao automático
                focusLocked = false
            }
        }
    }
```

- [ ] **Step 3: Compilar**

Run: `xcodebuild -project Knobler.xcodeproj -scheme Knobler -configuration Debug build 2>&1 | tail -3`
Expected: `** BUILD SUCCEEDED **`

- [ ] **Step 4: Commit**

```bash
git add Knobler/NotchViewModel.swift
git commit -m "feat: foco, trava e altura publicada no NotchViewModel"
```

---

### Task 4: Ajustes — a ordem-base

**Files:**
- Modify: `Knobler/AppSettings.swift`
- Modify: `Knobler/SettingsView.swift` (painel `.notch`)

**Interfaces:**
- Consumes: `NotchSection`, `NotchSectionOrder.sanear` (Task 1)
- Produces: `AppSettings.shared.notchSectionOrder: [NotchSection]`

- [ ] **Step 1: Persistir a ordem**

Em `Knobler/AppSettings.swift`, junto dos outros `@Published`:

```swift
    /// Ordem-base das seções do card aberto. A promoção por evento reordena
    /// isto na abertura; aqui fica só a preferência em repouso.
    @Published var notchSectionOrder: [NotchSection] {
        didSet {
            UserDefaults.standard.set(notchSectionOrder.map(\.rawValue),
                                      forKey: "notchSectionOrder")
        }
    }
```

E no `init()`, junto das outras leituras:

```swift
        notchSectionOrder = NotchSectionOrder.sanear(
            salva: UserDefaults.standard.stringArray(forKey: "notchSectionOrder") ?? [])
```

- [ ] **Step 2: Nome de exibição de cada seção**

Em `Knobler/NotchSectionOrder.swift`, acrescentar ao enum:

```swift
    /// Rótulo em pt-BR pros Ajustes e o `aria` da faixa.
    var titulo: String {
        switch self {
        case .musica: return "Música"
        case .atividade: return "Atividade"
        case .pomodoro: return "Pomodoro"
        case .shelf: return "Prateleira"
        case .espelho: return "Espelho"
        case .mensagens: return "Mensagens"
        case .historico: return "Histórico"
        case .nota: return "Nota rápida"
        }
    }

    /// Símbolo SF da faixa de ícones e da lista de Ajustes.
    var simbolo: String {
        switch self {
        case .musica: return "music.note"
        case .atividade: return "arrow.trianglehead.2.clockwise"
        case .pomodoro: return "timer"
        case .shelf: return "tray.full.fill"
        case .espelho: return "person.crop.square"
        case .mensagens: return "bubble.left.and.bubble.right.fill"
        case .historico: return "bell.fill"
        case .nota: return "square.and.pencil"
        }
    }
```

- [ ] **Step 3: A lista arrastável no painel Notch**

Em `Knobler/SettingsView.swift`, dentro do `Form` do painel `.notch`, acrescentar uma seção:

```swift
            Section("Ordem das seções do card") {
                Text("A ordem em repouso. Algo que acabou de acontecer sobe sozinho por alguns segundos.")
                    .font(.caption)
                    .foregroundStyle(.secondary)
                List {
                    ForEach(settings.notchSectionOrder, id: \.self) { s in
                        Label(s.titulo, systemImage: s.simbolo)
                    }
                    .onMove { origem, destino in
                        settings.notchSectionOrder.move(fromOffsets: origem, toOffset: destino)
                    }
                }
                .frame(height: 220)
            }
```

- [ ] **Step 4: Compilar e olhar o painel**

Run:
```bash
xcodebuild -project Knobler.xcodeproj -scheme Knobler -configuration Debug build 2>&1 | tail -3
./build/DerivedData/Build/Products/Debug/Knobler.app/Contents/MacOS/Knobler --ajustes=notch
```
Expected: BUILD SUCCEEDED, e o painel abre com a lista de 8 seções arrastável. Arrastar uma linha e reabrir o painel deve manter a nova ordem (prova de que o `didSet` persistiu).

- [ ] **Step 5: Commit**

```bash
git add Knobler/AppSettings.swift Knobler/SettingsView.swift Knobler/NotchSectionOrder.swift
git commit -m "feat: ordem-base das seções configurável nos Ajustes"
```

---

### Task 5: A view — seção em foco + faixa de ícones

**Files:**
- Modify: `Knobler/NotchView.swift` (`expandedContent`, `currentSize`, remoção de `musicContent`/`pageDots`/`grabber`)

**Interfaces:**
- Consumes: `vm.secoes`, `vm.focus`, `vm.focar(_:)`, `vm.publicarAltura(_:)`, `vm.recalcularSecoes(_:travadaNaNota:)`

⚠️ `MessageStore` **não** tem `.shared` — a `MessagesView` recebe o store por
`@EnvironmentObject`. A `NotchView` precisa ganhar `@EnvironmentObject var
messages: MessageStore` pra saber se há conversa (é o `hasMensagens` do
Step 5). O harness de snapshot já injeta esse ambiente.
- Produces: `NotchView.alturaDaSecao(_:) -> CGFloat` (usada pelo `currentSize`)

Alturas — os mesmos números que hoje vivem espalhados no `currentSize`, agora com dono:

| Seção | Altura |
|---|---|
| musica | 118 |
| atividade | 60 |
| pomodoro | 128 |
| shelf | 76 |
| espelho | 202 |
| mensagens | 272 |
| historico | `HistoryListView.listHeight + 12` |
| nota | `Self.noteEditorHeight + 20` |

- [ ] **Step 1: A altura por seção**

Em `Knobler/NotchView.swift`, acrescentar:

```swift
    /// Cada seção manda na própria altura. Antes isto era uma soma
    /// combinatória no `currentSize` (`height += hasMusic || hasShelf ? 46 : 60`),
    /// que já tinha causado moldura menor que o conteúdo.
    static func alturaDaSecao(_ s: NotchSection) -> CGFloat {
        switch s {
        case .musica: return 118
        case .atividade: return 60
        case .pomodoro: return 128
        case .shelf: return 76
        case .espelho: return 202
        case .mensagens: return 272
        case .historico: return HistoryListView.listHeight + 12
        case .nota: return Self.noteEditorHeight + 20
        }
    }
```

- [ ] **Step 2: Trocar o corpo do card**

Substituir `expandedContent` inteiro por:

```swift
    @ViewBuilder
    private var expandedContent: some View {
        VStack(spacing: 8) {
            Group {
                switch vm.focus {
                case .nota: noteSection
                case .historico: HistoryListView(history: history,
                                                 onOpen: { vm.setExpandedDirect(false) })
                case .mensagens: MessagesView(vm: vm)
                case .espelho: mirrorSection
                case .shelf: ShelfRowView(shelf: shelf, vm: vm)
                case .atividade: if let a = vm.activity { activityRow(a) }
                case .pomodoro: if let p = vm.pomodoro { pomodoroSection(p) }
                case .musica, .none: musicSection
                }
            }
            .transition(.blurReplace)
            Spacer(minLength: 0)
            // a nota é modo exclusivo: enquanto o teclado está nos dedos a faixa
            // não pode oferecer um caminho pra fora do campo
            if vm.focus != .nota { sectionStrip }
        }
        .animation(.easeOut(duration: 0.3), value: vm.focus)
    }
```

- [ ] **Step 3: A faixa**

Substituir `pageDots` (e apagar `grabber`) por:

```swift
    /// Rodapé do card: as seções que não estão em foco, cada uma com um sinal
    /// vivo mínimo. Você perde o detalhe, não o glance.
    private var sectionStrip: some View {
        HStack(spacing: 10) {
            ForEach(vm.secoes, id: \.self) { s in
                Button {
                    withAnimation(.easeOut(duration: 0.22)) { vm.focar(s) }
                } label: {
                    ZStack {
                        Image(systemName: s.simbolo)
                            .font(.system(size: 9, weight: .semibold))
                            .foregroundStyle(.white.opacity(vm.focus == s ? 0.9 : 0.35))
                        sinalVivo(s)
                    }
                    // alvo de clique maior que o desenho
                    .padding(4)
                    .contentShape(Rectangle())
                }
                .buttonStyle(.plain)
                .accessibilityLabel(s.titulo)
            }
        }
    }

    /// O sinal de cada ícone: anel pra progresso, ponto pra "está rolando",
    /// contagem pra pilha.
    @ViewBuilder
    private func sinalVivo(_ s: NotchSection) -> some View {
        switch s {
        case .atividade:
            if let p = vm.activity?.progress {
                Circle()
                    .trim(from: 0, to: p)
                    .stroke(.white.opacity(0.7), lineWidth: 1.5)
                    .rotationEffect(.degrees(-90))
                    .frame(width: 14, height: 14)
            }
        case .musica:
            if media.state?.isPlaying == true {
                Circle().fill(.white.opacity(0.8))
                    .frame(width: 3, height: 3)
                    .offset(x: 6, y: -6)
            }
        case .shelf:
            contagem(shelf.items.count)
        case .historico:
            contagem(history.items.count)
        case .nota:
            if !note.text.isEmpty {
                Circle().fill(.white.opacity(0.8))
                    .frame(width: 3, height: 3)
                    .offset(x: 6, y: -6)
            }
        case .pomodoro, .espelho, .mensagens:
            EmptyView()
        }
    }

    @ViewBuilder
    private func contagem(_ n: Int) -> some View {
        if n > 0 {
            Text("\(n)")
                .font(.system(size: 7, weight: .bold))
                .foregroundStyle(.white.opacity(0.9))
                .offset(x: 7, y: -6)
        }
    }
```

- [ ] **Step 4: Encolher o `currentSize`**

No `switch mode` do `currentSize`, trocar o caso `.music` inteiro (hoje ~40 linhas de soma) por:

```swift
        case .music:
            // topo (câmera) + a seção em foco + a faixa do rodapé
            let chrome = topInset + 10 + 16
            let corpo = vm.focus.map { Self.alturaDaSecao($0) } ?? 118
            return CGSize(width: expandedSize.width, height: chrome + corpo)
```

- [ ] **Step 5: Recalcular na abertura e publicar a altura**

Em `interactiveNotch`, junto dos outros modificadores:

```swift
        .onChange(of: vm.expanded) { _, aberto in
            guard aberto else { return }
            vm.recalcularSecoes(
                vm.estadoDasSecoes(hasMusic: hasMusic,
                                   hasShelf: !shelf.items.isEmpty,
                                   hasHistory: !history.items.isEmpty,
                                   hasMensagens: !messages.threads.isEmpty,
                                   hasNota: noteVisible),
                travadaNaNota: vm.typingNote)
        }
        .onChange(of: currentSize) { _, novo in vm.publicarAltura(novo.height) }
```

- [ ] **Step 6: Apagar o que ficou órfão**

Remover de `NotchView.swift`: `musicContent`, `grabber`, `pageDots` e a constante `grabberBlock`. Remover de `NotchViewModel.swift`: `historyOpen`, o enum `NotchTab` e a propriedade `tab` — e corrigir os usos que quebrarem (o compilador aponta: `keyboardAllowed`, `setHover`, `MessagesView`).

Em `keyboardAllowed`, a condição `vm.tab == .messages && vm.expanded` vira:

```swift
            || (vm.focus == .mensagens && vm.expanded)
```

- [ ] **Step 7: Compilar e olhar**

Run:
```bash
xcodebuild -project Knobler.xcodeproj -scheme Knobler -configuration Debug build 2>&1 | tail -3
./tools/snapshot.sh
```
Expected: BUILD SUCCEEDED. Os snapshots ainda vão renderizar cenários antigos — a reescrita deles é a Task 7. Aqui só não pode **quebrar** o harness.

- [ ] **Step 8: Commit**

```bash
git add Knobler/NotchView.swift Knobler/NotchViewModel.swift
git commit -m "feat: card aberto mostra uma seção em foco + faixa de ícones

Some a pilha de ifs do musicContent e a aritmética combinatória do
currentSize; cada seção declara a própria altura."
```

---

### Task 6: Gestos

**Files:**
- Modify: `Knobler/NotchGesture.swift`
- Modify: `Knobler/KnoblerApp.swift:646-745` (`handleScroll`)
- Modify: `tools/historycheck.swift` (testa `verticalTarget`)

**Interfaces:**
- Consumes: `vm.cardHeight`, `vm.focarVizinho(avancando:)`, `vm.focus` (Tasks 3 e 5)

- [ ] **Step 1: Ajustar o teste do gesto**

Em `tools/historycheck.swift`, na função `testGesto()`, trocar a asserção do puxão longo. Onde hoje se espera `.history` para 130 pt, passar a esperar `.expanded`:

```swift
        // a cortina do histórico foi aposentada: o histórico virou uma seção
        // como as outras, e um segundo caminho pra ele seria redundante
        assert(NotchGesture.verticalTarget(accumY: 130, accumX: 0) == .expanded,
               "puxão longo não é mais cortina")
```

- [ ] **Step 2: Rodar e ver falhar**

Run:
```bash
xcrun swiftc -parse-as-library -swift-version 5 \
  Knobler/NotchNotification.swift Knobler/NotificationHistory.swift \
  Knobler/NotchGesture.swift tools/historycheck.swift \
  -o /tmp/historycheck && /tmp/historycheck
```
Expected: FALHA na asserção "puxão longo não é mais cortina".

- [ ] **Step 3: Aposentar o alvo `.history`**

Em `Knobler/NotchGesture.swift`:

```swift
enum ScrollTarget: Equatable { case closed, expanded }
```

E em `verticalTarget`, remover a linha `if accumY > 120 { return .history }` e o parágrafo do doc-comment que descrevia os 120 pt.

- [ ] **Step 4: Rodar e ver passar**

Run: mesmo comando do Step 2.
Expected: `✅ historycheck ok`

- [ ] **Step 5: Reescrever o `handleScroll`**

Em `Knobler/KnoblerApp.swift`, a zona do gesto passa a derivar da altura publicada:

```swift
        // a altura vem do VM: cada seção declara a sua, e a tabela de literais
        // que morava aqui já ficava 2 pt do card da nota
        let zoneHeight: CGFloat = expanded ? vm.cardHeight + 12 : vm.notchSize.height + 10
```

O `switch target` perde o caso `.history`:

```swift
            switch target {
            case .closed:
                vm.setExpandedDirect(false)
            case .expanded:
                vm.setExpandedDirect(true)
            }
```

O handoff de scroll passa a olhar o foco:

```swift
            scrollStartedInHistory = vm.focus == .historico
                && !NotificationHistory.shared.items.isEmpty
                && !QuickNote.shared.hosted(by: id)
```

E o eixo horizontal com o card aberto passa a percorrer a faixa:

```swift
        } else if !scrollActed, abs(scrollAccumX) > 50 {
            scrollActed = true
            if expanded {
                // card aberto: horizontal anda na faixa de seções. Com a nota
                // em foco o eixo fica de fora — o campo é o card inteiro e
                // trocar de seção por baixo dele tiraria o teclado do usuário
                // sem ele ver.
                if vm.focus != .nota {
                    withAnimation(.easeOut(duration: 0.22)) {
                        vm.focarVizinho(avancando: scrollAccumX < 0)
                    }
                }
            } else {
                // (o ramo de pular faixa de música no notch fechado fica como está)
            }
        }
```

- [ ] **Step 6: Compilar e rodar o gate**

Run:
```bash
xcodebuild -project Knobler.xcodeproj -scheme Knobler -configuration Debug build 2>&1 | tail -3
./tools/check.sh
```
Expected: BUILD SUCCEEDED e `✅ 17 checks ok`

- [ ] **Step 7: Teste manual do gesto**

Run: `open build/DerivedData/Build/Products/Debug/Knobler.app`

Verificar, com música tocando e ao menos um arquivo na prateleira:
1. Swipe de dois dedos pra baixo abre o card. Um puxão longo **não** abre mais cortina nenhuma.
2. Swipe horizontal troca a seção em foco e o card muda de altura.
3. Com o histórico em foco, o scroll vertical rola a lista em vez de fechar o card.
4. Com a nota em foco, o swipe horizontal não faz nada e o texto digitado continua chegando no campo.

- [ ] **Step 8: Commit**

```bash
git add Knobler/NotchGesture.swift Knobler/KnoblerApp.swift tools/historycheck.swift
git commit -m "feat: gestos do card seguem a faixa de seções

Zona do scroll passa a ler vm.cardHeight (a tabela de literais já ficava a
2 pt de estourar no card da nota); horizontal percorre a faixa; a cortina
do histórico é aposentada."
```

---

### Task 7: API, snapshots e documentação

**Files:**
- Modify: `Knobler/NotchAPIServer.swift` (handler de `GET /status`)
- Modify: `tools/main.swift` (cenários do harness)
- Modify: `docs/architecture.md`, `docs/now-playing.md`, `docs/notifications.md`, `docs/messages.md`
- Modify: `CHANGELOG.md`

- [ ] **Step 1: Expor o foco no `/status`**

Em `Knobler/NotchAPIServer.swift`, no JSON de resposta do `GET /status`, acrescentar o campo:

```swift
        // qual seção o card está mostrando — dá pra um script perguntar o que
        // o notch tem na cara agora
        "focus": vm.focus?.rawValue ?? "",
```

- [ ] **Step 2: Reescrever os cenários do harness**

Em `tools/main.swift`, os cenários do card aberto deixam de ser combinações de estado e passam a ser uma seção em foco cada. Substituir os cenários `expanded-*` por:

Os cenários seguem a forma que já existe no arquivo — `Scenario(name:realNotch:) { vm, media, shelf in }`:

```swift
    Scenario(name: "foco-musica", realNotch: true) { vm, media, _ in
        media.injectPreview(state: fakeState(), artwork: fakeArtwork())
        vm.expanded = true
        vm.secoes = [.musica, .atividade, .shelf]
        vm.focus = .musica
    },
    Scenario(name: "foco-atividade", realNotch: true) { vm, media, _ in
        media.injectPreview(state: fakeState(), artwork: fakeArtwork())
        vm.activity = NotchActivity(id: "deploy", title: "Deploy", detail: "3 de 8",
                                    progress: 0.375, updatedAt: Date())
        vm.expanded = true
        vm.secoes = [.atividade, .musica]
        vm.focus = .atividade
    },
    Scenario(name: "foco-pomodoro", realNotch: true) { vm, media, _ in
        media.injectPreview(state: fakeState(), artwork: fakeArtwork())
        vm.pomodoro = PomodoroState(phase: .focus, runState: .running, remaining: 900,
                                    completedFocus: 1, cyclesUntilLong: 4)
        vm.expanded = true
        vm.secoes = [.pomodoro, .musica]
        vm.focus = .pomodoro
    },
    Scenario(name: "foco-shelf", realNotch: true) { vm, _, shelf in
        fakeFiles().forEach { shelf.add($0) }
        vm.expanded = true
        vm.secoes = [.shelf, .musica]
        vm.focus = .shelf
    },
```

**Não** criar cenário para `historico` nem `mensagens` (ambas usam `ScrollView`, que não renderiza offscreen — a área sai preta) nem para `espelho` (precisa de câmera real). Esses três seguem de captura manual no app rodando, como já documentado no CLAUDE.md.

- [ ] **Step 3: Rodar o harness e olhar os PNGs**

Run: `./tools/snapshot.sh`
Expected: os quatro `foco-*.png` novos existem em `Snapshots/`. Abrir cada um e conferir: a seção certa ocupa o card, a faixa aparece no rodapé com os ícones das outras, e o conteúdo não vaza da moldura.

- [ ] **Step 4: Atualizar a documentação**

Em `docs/architecture.md`, na descrição do notch expandido, substituir a menção à pilha de seções e às abas por: card mostra uma seção em foco, ordem-base nos Ajustes promovida por evento recente, faixa de ícones no rodapé.

Em `docs/now-playing.md`, `docs/notifications.md` e `docs/messages.md`, remover as menções a "aba", "pontinhos de página", "swipe de dois dedos troca de aba" e "cortina do histórico", trocando pelo vocabulário novo (seção, faixa, foco).

- [ ] **Step 5: CHANGELOG**

Acrescentar em `## [Unreleased]`:

```markdown
### Changed
- **Card do notch mostra uma coisa de cada vez**: em vez de empilhar música,
  atividade, prateleira e Pomodoro numa ordem fixa, o card agora mostra uma
  seção em foco e deixa as outras como ícones no rodapé, cada uma com um sinal
  vivo (anel de progresso, contagem, ponto de tocando). A ordem em repouso é
  sua — arrastável nos Ajustes › Notch — e o que acabou de acontecer sobe
  sozinho por alguns segundos. Clicar num ícone (ou deslizar na horizontal)
  trava o foco até o notch recolher. A cortina do histórico e as abas saíram:
  histórico e Mensagens viraram seções como as outras.
```

- [ ] **Step 6: Gate completo + commit**

```bash
./tools/check.sh
git add Knobler/NotchAPIServer.swift Knobler/NotchViewModel.swift tools/main.swift docs CHANGELOG.md
git commit -m "feat: /status expõe o foco; snapshots e docs acompanham a faixa"
```
Expected: `✅ 17 checks ok` antes do commit.

---

## Fora deste plano

- Prioridade do notch fechado (`NotchViewModel.mode`) — inalterada.
- Frequência aprendida / persistência de uso.
- Contagem de não-lidas real em `MessageStore` (hoje não existe noção de lido; a faixa usa a contagem de threads).
