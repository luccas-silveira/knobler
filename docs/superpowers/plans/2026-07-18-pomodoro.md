# Pomodoro Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Um timer Pomodoro clássico (foco → pausa → …) controlado pelo menu da barra, visível como pílula própria no notch, que para no fim de cada fase e espera o usuário iniciar a próxima.

**Architecture:** Um engine puro (`Pomodoro`, molde do `CalendarCountdown`) roda um `Timer` de 1s e publica um `PomodoroState` pro `NotchViewModel` via callback. O notch ganha um `Mode.pomodoro` compacto (abaixo de HUD/notificação na prioridade, então HUDs interrompem e voltam de graça; acima de `closed`, então hover ainda abre o card de música). O `AppDelegate` fia o engine, o menu dinâmico e a notificação+som de fim de fase. Tempo por relógio de parede (`endDate` absoluto), estado in-memory.

**Tech Stack:** Swift 5 / SwiftUI / AppKit, macOS 14.2+. Sem dependências novas. Gates do projeto: self-check `#if` com `assert`, build Debug via `xcodegen`+`xcodebuild`, harness de snapshot em `tools/`.

## Global Constraints

- **Deployment target:** macOS 14.2 — nada de API acima disso (sem `glassEffect`, sem SF Symbols > 14.x).
- **Zero dependências novas** — só Foundation/AppKit/SwiftUI.
- **`Pomodoro.swift` importa só Foundation** — sem SwiftUI/AppKit/AppSettings, pra compilar isolado no self-check e no harness de snapshot.
- **Tempo = relógio de parede:** regressivo sempre `endDate − Date()`; nunca acumular ticks. Estado in-memory (some ao fechar o app).
- **Idioma:** toda string de UI e comentário em português do Brasil, com acentuação correta.
- **Snapshot é o gate visual:** toda mudança de UI passa por `tools/snapshot.sh` + olhar os PNGs.
- **Commits:** mensagem em português, curta, no estilo do repo. Cada commit deste plano acrescenta o rodapé padrão de sessão (`Co-Authored-By:` + `Claude-Session:`) exigido pelo ambiente.

---

## File Structure

- **`Knobler/Pomodoro.swift`** (novo) — tipos (`PomodoroPhase`, `PomodoroRunState`, `PomodoroState`), engine `Pomodoro` (Timer, ações, lógica pura `advance`/`duration`, `Config`), e `selfCheck()`. Foundation only.
- **`Knobler/NotchViewModel.swift`** (modificar) — `@Published var pomodoro`, `case pomodoro` no `Mode` e sua prioridade.
- **`Knobler/NotchView.swift`** (modificar) — `pomodoroPill` + `case .pomodoro` no render/`currentSize`/`compact`.
- **`Knobler/AppSettings.swift`** (modificar) — 5 prefs + `Section("Pomodoro")`.
- **`Knobler/KnoblerApp.swift`** (modificar) — instancia o engine, fia `onState`/`onPhaseEnd`/`configProvider`, menu dinâmico via `NSMenuDelegate`, `Pomodoro.Config.fromSettings()`.
- **`tools/snapshot.sh`** (modificar) — adiciona `Knobler/Pomodoro.swift` à lista do `swiftc`.
- **`tools/main.swift`** (modificar) — cenários de snapshot do Pomodoro.

---

## Task 1: Engine, tipos e self-check (`Pomodoro.swift`)

**Files:**
- Create: `Knobler/Pomodoro.swift`
- Test: self-check embutido, rodável isolado via `-D POMODORO_SELFCHECK`

**Interfaces:**
- Consumes: nada (Foundation only).
- Produces:
  - `enum PomodoroPhase: Equatable { case focus, shortBreak, longBreak }`
  - `enum PomodoroRunState: Equatable { case idle, running, paused, waiting }`
  - `struct PomodoroState: Equatable { var phase: PomodoroPhase; var runState: PomodoroRunState; var remaining: TimeInterval }`
  - `final class Pomodoro` com:
    - `struct Config: Equatable { var focus: TimeInterval; var shortBreak: TimeInterval; var longBreak: TimeInterval; var cyclesUntilLong: Int }`
    - `var configProvider: () -> Config`
    - `var onState: ((PomodoroState?) -> Void)?`
    - `var onPhaseEnd: ((PomodoroPhase, PomodoroPhase) -> Void)?`
    - `private(set) var phase: PomodoroPhase`, `private(set) var runState: PomodoroRunState`
    - `func start()`, `func startNext()`, `func pause()`, `func resume()`, `func skip()`, `func reset()`
    - `static func advance(from:completedFocus:cyclesUntilLong:counts:) -> (PomodoroPhase, Int)`
    - `static func duration(of:config:) -> TimeInterval`
    - `static func selfCheck()`

- [ ] **Step 1: Escrever o self-check primeiro (arquivo só com a lógica pura + asserts)**

Cria `Knobler/Pomodoro.swift` com APENAS os tipos, a lógica pura e o self-check (o engine com Timer entra no Step 3). Isso deixa o self-check compilar e rodar isolado já falhando de propósito antes de existir a `advance`.

```swift
//
//  Pomodoro.swift
//  Knobler
//
//  Engine do timer Pomodoro: foco → pausa → …, para no fim de cada fase e
//  espera o usuário iniciar a próxima. Só Foundation (compila isolado pro
//  self-check e pro harness de snapshot). Tempo por relógio de parede.
//

import Foundation

enum PomodoroPhase: Equatable { case focus, shortBreak, longBreak }
enum PomodoroRunState: Equatable { case idle, running, paused, waiting }

/// Estado publicado pro ViewModel. nil = idle (sem pílula).
struct PomodoroState: Equatable {
    var phase: PomodoroPhase
    var runState: PomodoroRunState
    var remaining: TimeInterval
}

extension Pomodoro {
    /// Próxima fase + contador de focos, dado o que acabou. `counts` = true só na
    /// conclusão NATURAL de um foco; skip passa false (não conta, nunca dá pausa longa).
    static func advance(from ended: PomodoroPhase, completedFocus: Int,
                        cyclesUntilLong: Int, counts: Bool) -> (PomodoroPhase, Int) {
        switch ended {
        case .focus:
            let c = counts ? completedFocus + 1 : completedFocus
            let long = counts && c % max(1, cyclesUntilLong) == 0
            return (long ? .longBreak : .shortBreak, c)
        case .shortBreak, .longBreak:
            return (.focus, completedFocus)
        }
    }

    static func duration(of phase: PomodoroPhase, config: Config) -> TimeInterval {
        switch phase {
        case .focus: return config.focus
        case .shortBreak: return config.shortBreak
        case .longBreak: return config.longBreak
        }
    }

    static func selfCheck() {
        // foco natural 1..3 (cyclesUntilLong=4) → pausa curta, contador sobe
        assert(advance(from: .focus, completedFocus: 0, cyclesUntilLong: 4, counts: true) == (.shortBreak, 1))
        assert(advance(from: .focus, completedFocus: 2, cyclesUntilLong: 4, counts: true) == (.shortBreak, 3))
        // 4º foco natural → pausa longa
        assert(advance(from: .focus, completedFocus: 3, cyclesUntilLong: 4, counts: true) == (.longBreak, 4))
        // qualquer pausa → foco, contador intacto
        assert(advance(from: .shortBreak, completedFocus: 4, cyclesUntilLong: 4, counts: true) == (.focus, 4))
        assert(advance(from: .longBreak, completedFocus: 4, cyclesUntilLong: 4, counts: true) == (.focus, 4))
        // skip de um foco NÃO conta e nunca dá pausa longa
        assert(advance(from: .focus, completedFocus: 3, cyclesUntilLong: 4, counts: false) == (.shortBreak, 3))
    }
}

// Entrada standalone via @main (DECLARAÇÃO, não expressão top-level): assim o
// arquivo entra como biblioteca no harness multi-arquivo (Task 2) sem o erro
// "expressions are not allowed at the top level". Compilar com -parse-as-library.
#if POMODORO_SELFCHECK
@main
enum PomodoroSelfCheck {
    static func main() {
        Pomodoro.selfCheck()
        print("pomodoro self-check ok")
    }
}
#endif
```

- [ ] **Step 2: Rodar o self-check e ver falhar**

Run: `swiftc -parse-as-library -D POMODORO_SELFCHECK Knobler/Pomodoro.swift -o build/pomcheck`
Expected: FALHA de compilação — `use of unresolved identifier 'Pomodoro'` / `Config` (a classe `Pomodoro` e `Config` ainda não existem; só o `extension Pomodoro` foi escrito).

- [ ] **Step 3: Implementar o engine (classe `Pomodoro` completa)**

Adiciona a classe `Pomodoro` ANTES do `extension Pomodoro` no arquivo (ou logo após os tipos — Swift não liga com a ordem). Arquivo final:

```swift
//
//  Pomodoro.swift
//  Knobler
//
//  Engine do timer Pomodoro: foco → pausa → …, para no fim de cada fase e
//  espera o usuário iniciar a próxima. Só Foundation (compila isolado pro
//  self-check e pro harness de snapshot). Tempo por relógio de parede.
//

import Foundation

enum PomodoroPhase: Equatable { case focus, shortBreak, longBreak }
enum PomodoroRunState: Equatable { case idle, running, paused, waiting }

/// Estado publicado pro ViewModel. nil = idle (sem pílula).
struct PomodoroState: Equatable {
    var phase: PomodoroPhase
    var runState: PomodoroRunState
    var remaining: TimeInterval
}

final class Pomodoro {
    struct Config: Equatable {
        var focus: TimeInterval
        var shortBreak: TimeInterval
        var longBreak: TimeInterval
        var cyclesUntilLong: Int
    }

    /// Config lida na hora de cada fase — mudar em Ajustes vale na próxima fase.
    var configProvider: () -> Config = {
        .init(focus: 25 * 60, shortBreak: 5 * 60, longBreak: 15 * 60, cyclesUntilLong: 4)
    }
    /// Publica o estado (nil = idle). O AppDelegate espelha em vm.pomodoro.
    var onState: ((PomodoroState?) -> Void)?
    /// Fim de fase (fase que acabou, próxima) — AppDelegate notifica + som.
    var onPhaseEnd: ((PomodoroPhase, PomodoroPhase) -> Void)?

    private(set) var phase: PomodoroPhase = .focus
    private(set) var runState: PomodoroRunState = .idle
    private(set) var completedFocusSessions = 0
    private var endDate: Date?
    private var pausedRemaining: TimeInterval?
    private var timer: Timer?

    // MARK: - Ações

    /// Começa um Pomodoro do zero: foco rodando, contador zerado.
    func start() {
        completedFocusSessions = 0
        beginRunning(phase: .focus)
    }

    /// Sai da espera e roda a fase corrente (que já aponta pra próxima).
    func startNext() {
        guard runState == .waiting else { return }
        beginRunning(phase: phase)
    }

    func pause() {
        guard runState == .running, let end = endDate else { return }
        pausedRemaining = max(0, end.timeIntervalSinceNow)
        endDate = nil
        runState = .paused
        stopTimer()
        publish()
    }

    func resume() {
        guard runState == .paused, let rem = pausedRemaining else { return }
        endDate = Date().addingTimeInterval(rem)
        pausedRemaining = nil
        runState = .running
        startTimer()
        publish()
    }

    /// Pular = próxima fase agora, já rodando. Não conta o foco corrente.
    func skip() {
        guard runState != .idle else { return }
        let (next, count) = Pomodoro.advance(
            from: phase, completedFocus: completedFocusSessions,
            cyclesUntilLong: configProvider().cyclesUntilLong, counts: false)
        completedFocusSessions = count
        beginRunning(phase: next)
    }

    func reset() {
        stopTimer()
        runState = .idle
        phase = .focus
        completedFocusSessions = 0
        endDate = nil
        pausedRemaining = nil
        onState?(nil)
    }

    // MARK: - Interno

    private func beginRunning(phase newPhase: PomodoroPhase) {
        phase = newPhase
        endDate = Date().addingTimeInterval(Pomodoro.duration(of: newPhase, config: configProvider()))
        pausedRemaining = nil
        runState = .running
        startTimer()
        publish()
    }

    private func currentRemaining() -> TimeInterval {
        if let end = endDate { return max(0, end.timeIntervalSinceNow) }
        return pausedRemaining ?? 0
    }

    private func startTimer() {
        stopTimer()
        // Timer(...) não auto-agenda; add só uma vez em .common pra tickar durante
        // tracking do menu / interação no notch. scheduledTimer duplicaria o tick.
        let t = Timer(timeInterval: 1, repeats: true) { [weak self] _ in self?.tick() }
        RunLoop.main.add(t, forMode: .common)
        timer = t
    }

    private func stopTimer() {
        timer?.invalidate()
        timer = nil
    }

    private func tick() {
        guard runState == .running, let end = endDate else { return }
        if Date() >= end {
            let ended = phase
            let (next, count) = Pomodoro.advance(
                from: ended, completedFocus: completedFocusSessions,
                cyclesUntilLong: configProvider().cyclesUntilLong, counts: true)
            completedFocusSessions = count
            phase = next
            runState = .waiting
            endDate = nil
            stopTimer()
            onPhaseEnd?(ended, next)
            publish()
        } else {
            publish()
        }
    }

    private func publish() {
        let rem: TimeInterval
        switch runState {
        case .idle: onState?(nil); return
        case .waiting: rem = Pomodoro.duration(of: phase, config: configProvider())
        case .paused: rem = pausedRemaining ?? 0
        case .running: rem = currentRemaining()
        }
        onState?(PomodoroState(phase: phase, runState: runState, remaining: rem))
    }
}

extension Pomodoro {
    /// Próxima fase + contador de focos, dado o que acabou. `counts` = true só na
    /// conclusão NATURAL de um foco; skip passa false (não conta, nunca dá pausa longa).
    static func advance(from ended: PomodoroPhase, completedFocus: Int,
                        cyclesUntilLong: Int, counts: Bool) -> (PomodoroPhase, Int) {
        switch ended {
        case .focus:
            let c = counts ? completedFocus + 1 : completedFocus
            let long = counts && c % max(1, cyclesUntilLong) == 0
            return (long ? .longBreak : .shortBreak, c)
        case .shortBreak, .longBreak:
            return (.focus, completedFocus)
        }
    }

    static func duration(of phase: PomodoroPhase, config: Config) -> TimeInterval {
        switch phase {
        case .focus: return config.focus
        case .shortBreak: return config.shortBreak
        case .longBreak: return config.longBreak
        }
    }

    static func selfCheck() {
        // foco natural 1..3 (cyclesUntilLong=4) → pausa curta, contador sobe
        assert(advance(from: .focus, completedFocus: 0, cyclesUntilLong: 4, counts: true) == (.shortBreak, 1))
        assert(advance(from: .focus, completedFocus: 2, cyclesUntilLong: 4, counts: true) == (.shortBreak, 3))
        // 4º foco natural → pausa longa
        assert(advance(from: .focus, completedFocus: 3, cyclesUntilLong: 4, counts: true) == (.longBreak, 4))
        // qualquer pausa → foco, contador intacto
        assert(advance(from: .shortBreak, completedFocus: 4, cyclesUntilLong: 4, counts: true) == (.focus, 4))
        assert(advance(from: .longBreak, completedFocus: 4, cyclesUntilLong: 4, counts: true) == (.focus, 4))
        // skip de um foco NÃO conta e nunca dá pausa longa
        assert(advance(from: .focus, completedFocus: 3, cyclesUntilLong: 4, counts: false) == (.shortBreak, 3))
    }
}

// Entrada standalone via @main (DECLARAÇÃO, não expressão top-level): assim o
// arquivo entra como biblioteca no harness multi-arquivo (Task 2) sem o erro
// "expressions are not allowed at the top level". Compilar com -parse-as-library.
#if POMODORO_SELFCHECK
@main
enum PomodoroSelfCheck {
    static func main() {
        Pomodoro.selfCheck()
        print("pomodoro self-check ok")
    }
}
#endif
```

- [ ] **Step 4: Rodar o self-check e ver passar**

Run: `swiftc -parse-as-library -D POMODORO_SELFCHECK Knobler/Pomodoro.swift -o build/pomcheck && ./build/pomcheck`
Expected: imprime `pomodoro self-check ok` e sai 0 (sem `-O`, os `assert` ficam ativos).

- [ ] **Step 5: Commit**

```bash
git add Knobler/Pomodoro.swift
git commit -m "Pomodoro: engine (fases, relógio de parede, self-check)"
```

---

## Task 2: Estado no ViewModel + prioridade de modo (`NotchViewModel.swift`)

**Files:**
- Modify: `Knobler/NotchViewModel.swift` (props publicadas ~linha 41-52; `enum Mode` ~linha 61-63; `var mode` ~linha 66-72)
- Modify: `tools/snapshot.sh` (lista do `swiftc`)

**Interfaces:**
- Consumes (da Task 1): `PomodoroState`, `PomodoroPhase`, `PomodoroRunState`.
- Produces: `NotchViewModel.pomodoro: PomodoroState?`, `NotchViewModel.Mode.pomodoro`.

- [ ] **Step 1: Adicionar a propriedade publicada**

Em `NotchViewModel`, logo após `@Published var activity: NotchActivity?` (linha 44), adiciona:

```swift
    /// Timer Pomodoro em exibição (pílula própria no notch). nil = idle.
    @Published var pomodoro: PomodoroState?
```

- [ ] **Step 2: Adicionar o case ao enum `Mode`**

Troca a linha `case closed, music, notification, hud, dictation, question` por:

```swift
        case closed, music, notification, hud, dictation, question, pomodoro
```

- [ ] **Step 3: Inserir o Pomodoro na prioridade de `mode`**

Troca o corpo do `var mode: Mode` (o `return expanded ? .music : .closed` no fim) por esta ordem — Pomodoro fica ABAIXO de music (hover ainda abre o card de música) e ACIMA de closed (toma conta quando fechado):

```swift
    var mode: Mode {
        if ask != nil { return .question }
        if dictation != nil { return .dictation }
        if activeNotification != nil { return .notification }
        if hud != nil { return .hud }
        if expanded { return .music }
        if pomodoro != nil { return .pomodoro }
        return .closed
    }
```

- [ ] **Step 4: Incluir `Pomodoro.swift` no harness de snapshot**

Em `tools/snapshot.sh`, na lista de arquivos do `swiftc`, adiciona a linha do `Pomodoro.swift` logo após `Knobler/NotchViewModel.swift \`:

```bash
  Knobler/NotchViewModel.swift \
  Knobler/Pomodoro.swift \
  Knobler/Ask.swift \
```

- [ ] **Step 5: Compilar o harness pra verificar que os tipos resolvem**

Run: `tools/snapshot.sh`
Expected: compila e re-renderiza os PNGs existentes sem erro (o `#if POMODORO_SELFCHECK` fica fora sem a flag, então `Pomodoro.swift` entra como arquivo de biblioteca, sem conflito com o `main.swift`). Nenhum erro de `PomodoroState`/`Mode.pomodoro` não encontrado.

- [ ] **Step 6: Commit**

```bash
git add Knobler/NotchViewModel.swift tools/snapshot.sh
git commit -m "Pomodoro: estado no ViewModel + prioridade de modo"
```

---

## Task 3: Pílula no notch + cenários de snapshot (`NotchView.swift`, `tools/main.swift`)

> **Correção (execução):** a Task 2 já adicionou `case .pomodoro` STUB nos dois `switch vm.mode` do `NotchView` (render → `EmptyView()`; `currentSize` → `notchSize`), porque adicionar `Mode.pomodoro` quebra o switch exaustivo e o harness precisava compilar. Esta task **substitui** essas stubs pela pílula real e pelo sizing real — não adiciona um novo case.

**Files:**
- Modify: `Knobler/NotchView.swift` (`compact` ~linha 52; switch de `notch` ~linha 61-92; `currentSize` ~linha 158-162; nova view `pomodoroPill`)
- Modify: `tools/main.swift` (lista `scenarios`, antes do `]` de fechamento)

**Interfaces:**
- Consumes: `vm.pomodoro` (`PomodoroState?`), `PomodoroPhase`, `PomodoroRunState`.
- Produces: `pomodoroPill` (private var), render do `case .pomodoro`.

- [ ] **Step 1: Adicionar os cenários de snapshot (o "teste" visual, primeiro)**

Em `tools/main.swift`, adiciona estes 4 cenários dentro do array `scenarios`, logo antes do `]` que fecha o array (após o cenário `ask-paged`):

```swift
    Scenario(name: "pomodoro-focus", realNotch: true) { vm, _ in
        vm.pomodoro = PomodoroState(phase: .focus, runState: .running, remaining: 23 * 60 + 14)
    },
    Scenario(name: "pomodoro-break", realNotch: true) { vm, _ in
        vm.pomodoro = PomodoroState(phase: .shortBreak, runState: .running, remaining: 4 * 60 + 32)
    },
    Scenario(name: "pomodoro-paused", realNotch: true) { vm, _ in
        vm.pomodoro = PomodoroState(phase: .focus, runState: .paused, remaining: 12 * 60 + 3)
    },
    Scenario(name: "pomodoro-waiting", realNotch: false) { vm, _ in
        vm.pomodoro = PomodoroState(phase: .shortBreak, runState: .waiting, remaining: 5 * 60)
    },
```

- [ ] **Step 2: Rodar o snapshot e ver falhar**

Run: `tools/snapshot.sh`
Expected: FALHA de compilação — `NotchView` não trata `case .pomodoro` no switch de `notch` (switch exaustivo). É o esperado antes de renderizar a pílula.

- [ ] **Step 3: Adicionar o `case .pomodoro` ao render e ao sizing**

Em `NotchView.swift`, no `private var notch`, troca a linha do `compact` (linha ~52):

```swift
        let compact = vm.mode == .closed || vm.mode == .hud
            || vm.mode == .dictation || vm.mode == .pomodoro
```

No `switch vm.mode` desse mesmo `notch`, adiciona o case logo após o bloco `case .dictation:`:

```swift
            case .pomodoro:
                pomodoroPill
                    .transition(.blurReplace)
```

No `private var currentSize`, inclui `.pomodoro` junto do case de hud/dictation (linha ~158). Troca `case .hud, .dictation:` por:

```swift
        case .hud, .dictation, .pomodoro:
```

- [ ] **Step 4: Implementar a `pomodoroPill`**

Adiciona no `NotchView` (perto da `dictationPill`, após a linha 261) a view e os helpers. Ícone+rótulo à esquerda, tempo à direita, vão morto no meio (cai sobre a câmera no notch real, igual ao hudPill). Cor por fase + rótulo distinto (acessibilidade: não depende só de cor):

```swift
    // MARK: - Pílula do Pomodoro

    @ViewBuilder
    private var pomodoroPill: some View {
        if let p = vm.pomodoro {
            let focus = p.phase == .focus
            let tint: Color = focus
                ? Color(red: 1.00, green: 0.45, blue: 0.32)   // tomate (foco)
                : Color(red: 0.30, green: 0.82, blue: 0.55)   // verde (pausa)
            HStack(spacing: 0) {
                HStack(spacing: 6) {
                    Image(systemName: focus ? "brain.head.profile" : "cup.and.saucer.fill")
                        .font(.footnote)
                        .foregroundStyle(tint)
                    if p.runState == .paused {
                        Image(systemName: "pause.fill")
                            .font(.system(size: 9, weight: .bold))
                            .foregroundStyle(.white.opacity(0.7))
                    }
                    if p.runState == .waiting {
                        Text(Self.pomodoroWaitingLabel(p.phase))
                            .font(.system(size: 12, weight: .semibold))
                            .foregroundStyle(.white)
                    }
                }
                .padding(.leading, vm.hasRealNotch ? 14 : 16)
                Spacer(minLength: 0)
                Text(Self.mmss(p.remaining))
                    .font(.system(size: 13, weight: .semibold, design: .rounded).monospacedDigit())
                    .foregroundStyle(.white)
                    .padding(.trailing, vm.hasRealNotch ? 14 : 16)
            }
            .frame(height: vm.notchSize.height)
        }
    }

    /// "Foco ▸" / "Pausa ▸" / "Pausa longa ▸" — a próxima ação na espera.
    private static func pomodoroWaitingLabel(_ phase: PomodoroPhase) -> String {
        switch phase {
        case .focus: return "Foco ▸"
        case .shortBreak: return "Pausa ▸"
        case .longBreak: return "Pausa longa ▸"
        }
    }

    private static func mmss(_ t: TimeInterval) -> String {
        let s = max(0, Int(t.rounded()))
        return String(format: "%02d:%02d", s / 60, s % 60)
    }
```

- [ ] **Step 5: Rodar o snapshot e ver passar**

Run: `tools/snapshot.sh`
Expected: compila e gera `Snapshots/pomodoro-focus.png`, `pomodoro-break.png`, `pomodoro-paused.png`, `pomodoro-waiting.png`.

- [ ] **Step 6: Olhar os PNGs (gate visual obrigatório)**

Run: `open Snapshots/pomodoro-focus.png Snapshots/pomodoro-break.png Snapshots/pomodoro-paused.png Snapshots/pomodoro-waiting.png`
Expected (conferir a olho):
- `focus`: ícone tomate + `23:14` à direita, vão no meio sobre a câmera.
- `break`: ícone verde xícara + `04:32`.
- `paused`: tomate + glifo de pause + `12:03`.
- `waiting`: verde + `Pausa ▸` + `05:00` (notch externo, sem câmera).
Se algum recorte cortar texto ou a cor não distinguir foco/pausa, ajustar paddings/tamanho antes de seguir.

- [ ] **Step 7: Commit**

```bash
git add Knobler/NotchView.swift tools/main.swift Snapshots/pomodoro-*.png
git commit -m "Pomodoro: pílula no notch + snapshots"
```

---

## Task 4: Ajustes (`AppSettings.swift`)

**Files:**
- Modify: `Knobler/AppSettings.swift` (props ~linha 66-72; `init` ~linha 88-107; `Section` na `SettingsView` ~linha 116-152)

**Interfaces:**
- Consumes: nada (só UserDefaults).
- Produces: `AppSettings.pomodoroFocus: Int`, `pomodoroShortBreak: Int`, `pomodoroLongBreak: Int`, `pomodoroCyclesLong: Int`, `pomodoroSound: Bool`.

- [ ] **Step 1: Adicionar as propriedades persistidas**

Em `AppSettings`, após `hideScreenshotPreview` (linha ~72), adiciona:

```swift
    // MARK: Pomodoro (durações em minutos)
    @Published var pomodoroFocus: Int {
        didSet { UserDefaults.standard.set(pomodoroFocus, forKey: "pomodoroFocus") }
    }
    @Published var pomodoroShortBreak: Int {
        didSet { UserDefaults.standard.set(pomodoroShortBreak, forKey: "pomodoroShortBreak") }
    }
    @Published var pomodoroLongBreak: Int {
        didSet { UserDefaults.standard.set(pomodoroLongBreak, forKey: "pomodoroLongBreak") }
    }
    @Published var pomodoroCyclesLong: Int {
        didSet { UserDefaults.standard.set(pomodoroCyclesLong, forKey: "pomodoroCyclesLong") }
    }
    /// Som curto ao trocar de fase (fim de foco/pausa).
    @Published var pomodoroSound: Bool {
        didSet { UserDefaults.standard.set(pomodoroSound, forKey: "pomodoroSound") }
    }
```

- [ ] **Step 2: Inicializar os defaults**

No `private init()`, após `hideScreenshotPreview = flag("hideScreenshotPreview")` (linha ~106), adiciona (usa o helper `flag` já existente pro bool default-true; ints com fallback quando 0/ausente):

```swift
        func intOr(_ key: String, _ d: Int) -> Int {
            let v = defaults.integer(forKey: key); return v == 0 ? d : v
        }
        pomodoroFocus = intOr("pomodoroFocus", 25)
        pomodoroShortBreak = intOr("pomodoroShortBreak", 5)
        pomodoroLongBreak = intOr("pomodoroLongBreak", 15)
        pomodoroCyclesLong = intOr("pomodoroCyclesLong", 4)
        pomodoroSound = flag("pomodoroSound")   // default true
```

- [ ] **Step 3: Adicionar a seção na tela de Ajustes**

Na `SettingsView.body`, após a `Section("Ditado")` e antes da `Section("Geral")`, adiciona:

```swift
            Section("Pomodoro") {
                Stepper("Foco: \(settings.pomodoroFocus) min",
                        value: $settings.pomodoroFocus, in: 1...120)
                Stepper("Pausa curta: \(settings.pomodoroShortBreak) min",
                        value: $settings.pomodoroShortBreak, in: 1...60)
                Stepper("Pausa longa: \(settings.pomodoroLongBreak) min",
                        value: $settings.pomodoroLongBreak, in: 1...60)
                Stepper("Focos até pausa longa: \(settings.pomodoroCyclesLong)",
                        value: $settings.pomodoroCyclesLong, in: 1...12)
                Toggle("Som ao trocar de fase", isOn: $settings.pomodoroSound)
            }
```

- [ ] **Step 4: Buildar Debug pra verificar**

Run: `xcodegen generate && xcodebuild -project Knobler.xcodeproj -scheme Knobler -configuration Debug build 2>&1 | tail -3`
Expected: `** BUILD SUCCEEDED **`.

- [ ] **Step 5: Commit**

```bash
git add Knobler/AppSettings.swift Knobler.xcodeproj
git commit -m "Pomodoro: durações e som nos Ajustes"
```

---

## Task 5: Fiação do engine + menu dinâmico (`KnoblerApp.swift`)

**Files:**
- Modify: `Knobler/KnoblerApp.swift` (props do `AppDelegate` ~linha 28-49; `applicationDidFinishLaunching` ~linha 67+, perto de `calendar.onActivity` linha 165; `setupStatusItem` ~linha 451-462)

**Interfaces:**
- Consumes: `Pomodoro`, `Pomodoro.Config`, `PomodoroPhase`, `PomodoroRunState` (Task 1); `vm.pomodoro`, `vm.enqueue` (Tasks 2 e existente); `AppSettings.pomodoro*` (Task 4).
- Produces: comportamento (menu, notificação, som). Nada consumido por tasks posteriores.

- [ ] **Step 1: Instanciar o engine e o mapeamento de config**

No `AppDelegate`, junto das outras dependências (após `private let calendar = CalendarCountdown()`, linha ~37), adiciona:

```swift
    private let pomodoro = Pomodoro()
```

No fim do arquivo (fora da classe, junto de outras extensões), adiciona o mapeamento de `AppSettings` → `Config` (mantém o `Pomodoro.swift` livre de `AppSettings`):

```swift
extension Pomodoro.Config {
    /// Lê as durações atuais dos Ajustes (minutos → segundos), clamp mínimo 1 min.
    static func fromSettings() -> Pomodoro.Config {
        let s = AppSettings.shared
        return .init(
            focus: TimeInterval(max(1, s.pomodoroFocus) * 60),
            shortBreak: TimeInterval(max(1, s.pomodoroShortBreak) * 60),
            longBreak: TimeInterval(max(1, s.pomodoroLongBreak) * 60),
            cyclesUntilLong: max(1, s.pomodoroCyclesLong)
        )
    }
}
```

- [ ] **Step 2: Fiar `configProvider`, `onState` e `onPhaseEnd`**

Em `applicationDidFinishLaunching`, junto da fiação do `calendar.onActivity` (linha ~165), adiciona:

```swift
        pomodoro.configProvider = { Pomodoro.Config.fromSettings() }
        pomodoro.onState = { [weak self] state in
            self?.notches.values.forEach { $0.viewModel.pomodoro = state }
        }
        pomodoro.onPhaseEnd = { [weak self] ended, next in
            guard let self else { return }
            let (title, body) = Self.pomodoroNotice(ended: ended, next: next)
            self.notches.values.forEach {
                $0.viewModel.enqueue(NotchNotification(appName: "Pomodoro", title: title, body: body))
            }
            if AppSettings.shared.pomodoroSound { NSSound(named: "Glass")?.play() }
        }
```

- [ ] **Step 3: Adicionar o texto da notificação de fim de fase**

Adiciona no `AppDelegate` (método estático privado, perto dos outros helpers):

```swift
    /// Título/corpo da notificação de fim de fase, conforme o que acabou e o que vem.
    private static func pomodoroNotice(ended: PomodoroPhase,
                                       next: PomodoroPhase) -> (String, String) {
        let s = AppSettings.shared
        switch next {
        case .focus:
            return ("Pausa acabou", "Bora focar — \(s.pomodoroFocus) min")
        case .shortBreak:
            return ("Foco concluído", "Hora da pausa — \(s.pomodoroShortBreak) min")
        case .longBreak:
            return ("Foco concluído", "Pausa longa — \(s.pomodoroLongBreak) min")
        }
    }
```

- [ ] **Step 4: Menu dinâmico via `NSMenuDelegate`**

Troca a assinatura da classe pra conformar `NSMenuDelegate`. Linha 21:

```swift
final class AppDelegate: NSObject, NSApplicationDelegate, NSMenuDelegate {
```

Troca o `setupStatusItem()` (linhas 451-462) por uma versão que delega a construção do menu:

```swift
    private func setupStatusItem() {
        let item = NSStatusBar.system.statusItem(withLength: NSStatusItem.variableLength)
        item.button?.title = "◐"
        let menu = NSMenu()
        menu.delegate = self   // itens do Pomodoro reconstroem a cada abertura
        item.menu = menu
        statusItem = item
    }

    // MARK: - Menu (reconstruído por estado do Pomodoro)

    func menuNeedsUpdate(_ menu: NSMenu) {
        menu.removeAllItems()
        let s = AppSettings.shared
        switch pomodoro.runState {
        case .idle:
            addPomodoroItem(menu, "▶ Iniciar foco (\(s.pomodoroFocus) min)", #selector(pomStart))
        case .running:
            addPomodoroItem(menu, "⏸ Pausar", #selector(pomPause))
            addPomodoroItem(menu, "⏭ Pular fase", #selector(pomSkip))
            addPomodoroItem(menu, "↺ Resetar", #selector(pomReset))
        case .paused:
            addPomodoroItem(menu, "▶ Retomar", #selector(pomResume))
            addPomodoroItem(menu, "⏭ Pular fase", #selector(pomSkip))
            addPomodoroItem(menu, "↺ Resetar", #selector(pomReset))
        case .waiting:
            let mins = Int(Pomodoro.duration(of: pomodoro.phase,
                                             config: .fromSettings()) / 60)
            let label = pomodoro.phase == .focus
                ? "▶ Iniciar foco (\(mins) min)"
                : "▶ Iniciar pausa (\(mins) min)"
            addPomodoroItem(menu, label, #selector(pomStartNext))
            addPomodoroItem(menu, "↺ Resetar", #selector(pomReset))
        }
        menu.addItem(.separator())
        let settings = menu.addItem(
            withTitle: "Ajustes…", action: #selector(openSettings), keyEquivalent: ",")
        settings.target = self
        menu.addItem(.separator())
        menu.addItem(withTitle: "Quit Knobler",
                     action: #selector(NSApplication.terminate(_:)), keyEquivalent: "q")
    }

    private func addPomodoroItem(_ menu: NSMenu, _ title: String, _ sel: Selector) {
        let it = menu.addItem(withTitle: title, action: sel, keyEquivalent: "")
        it.target = self
    }

    @objc private func pomStart() { pomodoro.start() }
    @objc private func pomStartNext() { pomodoro.startNext() }
    @objc private func pomPause() { pomodoro.pause() }
    @objc private func pomResume() { pomodoro.resume() }
    @objc private func pomSkip() { pomodoro.skip() }
    @objc private func pomReset() { pomodoro.reset() }
```

- [ ] **Step 5: Buildar Debug**

Run: `xcodegen generate && xcodebuild -project Knobler.xcodeproj -scheme Knobler -configuration Debug build 2>&1 | tail -3`
Expected: `** BUILD SUCCEEDED **`.

- [ ] **Step 6: E2E manual no app real**

Roda o app buildado (`open build/.../Knobler.app` ou pelo Xcode) e confere o roteiro:
1. Menu da barra → `▶ Iniciar foco (25 min)`. Pílula 🍅 aparece no notch tomando conta do fechado; regressivo corre.
2. Ajusta volume → HUD pisca ~1,5s e a pílula do Pomodoro volta sozinha.
3. Menu → `⏸ Pausar` → pílula mostra o glifo de pause e o número congela. `▶ Retomar` volta a correr.
4. Menu → `⏭ Pular fase` → emenda direto na pausa (verde), rodando.
5. Deixa uma fase acabar (ou põe foco = 1 min em Ajustes) → notificação "Foco concluído" desce no notch + som (se ligado); pílula vira `Pausa ▸ 5:00` e espera. Menu mostra `▶ Iniciar pausa (5 min)`.
6. `↺ Resetar` → pílula some, notch volta ao normal.
7. Fecha o app → nada persiste (relógio some).
Expected: todos os passos batem com o comportamento descrito.

- [ ] **Step 7: Commit**

```bash
git add Knobler/KnoblerApp.swift Knobler.xcodeproj
git commit -m "Pomodoro: fiação do engine, menu dinâmico e notificação de fase"
```

---

## Self-Review

**1. Spec coverage** (contra `POMODORO-SPEC.md`):
- Controle pelo menu → Task 5 (menu dinâmico). ✓
- Fases clássicas configuráveis (25/5/15, ciclos) → Task 1 (`advance`/`Config`) + Task 4 (Ajustes). ✓
- Pílula própria com número + cor por fase → Task 3 (`pomodoroPill`). ✓
- Toma conta do fechado; HUD interrompe e volta → Task 2 (prioridade: pomodoro abaixo de hud, acima de closed). ✓
- Para no fim da fase, avisa (notch + som), espera → Task 1 (`.waiting` + `onPhaseEnd`) + Task 5 (notificação + `NSSound`). ✓
- Pílula de espera com a próxima ação → Task 3 (`pomodoroWaitingLabel`). ✓
- Relógio de parede, some ao fechar → Task 1 (`endDate` absoluto, estado in-memory). ✓
- Skip não conta o foco / nunca dá pausa longa → Task 1 (`advance(counts:false)` + self-check). ✓
- Gate de snapshot → Task 3 (cenários + olhar PNGs). ✓
- Self-check obrigatório → Task 1 (`selfCheck` + Step 2/4). ✓

**2. Placeholder scan:** nenhum "TBD/TODO/etc." — todo passo com código real e comando com output esperado. ✓

**3. Type consistency:** `PomodoroState(phase:runState:remaining:)`, `PomodoroPhase`, `PomodoroRunState`, `Pomodoro.Config(focus:shortBreak:longBreak:cyclesUntilLong:)`, `advance(from:completedFocus:cyclesUntilLong:counts:)`, `duration(of:config:)`, `Config.fromSettings()`, `vm.pomodoro`, `Mode.pomodoro` — mesmos nomes em todas as tasks. Ações `start/startNext/pause/resume/skip/reset` batem entre Task 1 e os `@objc` da Task 5. ✓

**Nota de sequência:** o app só compila com `xcodebuild` a partir da Task 4 (Tasks 1-3 são verificadas por `swiftc` isolado e pelo harness de snapshot, que não incluem `KnoblerApp.swift`). A fiação que fecha o loop é a Task 5. Executar em ordem.
